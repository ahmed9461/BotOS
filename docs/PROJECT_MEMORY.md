# ذاكرة BotOS — نقطة الاستئناف

آخر تحديث: 2026-09-18. **أساس المعاينة اجتاز البناء، والتطبيق الحي لم يبدأ.** انتهت مهمة0002 لإصلاح CI، وأُغلقت بوابة بناء0001. هذا التحديث توثيق فقط؛ الكود المختبر عند `7724a5fbc1459aac58b8d829f6e4ded1b8281f71`.

## القواعد والمتطلبات الثابتة

قبل أي تنفيذ: اقرأ AGENTS وهذه الذاكرة والبنية والخارطة والخطة والكود المرتبط. افحص الفرع وPRs ونقطة الوصول. اكتب خطة مرتبة قبل التعديل، ثم نفذ واختبر ووثّق؛ لا تعِد إنشاء العمل ولا تعلن نجاحًا دون دليل.

BotOS يجمع بوتات Telegram في تبويبات قابلة للتسمية والترتيب ويعرض الرسائل والأزرار حسب أنواعها. لا AI يخمن callbacks ولا تكامل منفصل لكل بوت. إضافة بوت بنوع مدعوم لا تحتاج APK جديدًا؛ نوع بروتوكول جديد قد يحتاج تطوير المحرك.

عربية/RTL وEnglish/LTR، Dark/Light/System، Pearl/Graphite/Mauve بلا كحلي أو أخضر، أيقونات أصلية ناعمة وحركة قصيرة قابلة للمقاطعة وتقليل الحركة. لا نصوص هندسية في واجهة المستخدم.

## أين يوجد العمل؟

main يحتوي تخطيطP0 عند ca4776f5f9bdf7cdac757daddb036a9722776c22. كود التطبيق على `feat/android-foundation` داخل PR#1؛ لم يدمج وقت كتابة هذه الذاكرة. لا تحكم من main وحده ولا تبدأ المشروع من جديد.

## ما نُفذ بالفعل

خمس وحدات app/model/telegram/data/designsystem. Compose وNavigation3، ثيمات وأيقونة أصلية، DataStore للأسماء والترتيب والتفضيلات، نماذج رسائل وأفعال بعزل الحساب والبوت ومراجعة الرسالة، renderer ومعاينة محلية صريحة. إضافة/تعديل/ترتيب/حذف bookmarks وفتحها خارجيًا في Telegram. أسماء البوتات غير متحققة من Telegram بعد. PreviewGateway مصطنع؛ لا تسجيل دخول أو إرسال حي أو TDLib JNI. المسودات والتمرير مؤقتان.

## دليل التسليم الحالي

Run **35288442618** نجح بالكامل على الكود7724a5f. التقارير المقروءة: JUnit4 ناجحة (1 حالة جامعة تتضمن37 assertion للنواة +3 للمعاينة)، دون فشل/تخطي؛ Python13، contrast14، repo/XML/docs checks ناجحة. lint: **0 أخطاء و9 تحذيرات موثقة**. assembleDebug ورفع APK وWrapper وتقارير الاختبار نجحت.

APK: BotOS-0.1.0-preview.apk؛ 12,910,987 بايت. SHA256:
`b33a1093f52d0d6bb49ecfac88e05284d7b604cc3caa9c75330bf92fe526516b`.
Artifact10525521304. تم تنزيله والتحقق من تطابق بصمته مع SHA256SUMS ومن سلامة ZIP الداخلية. هذه سلامة نقل/تجميع لا تدقيق أمني شامل أو اختبار تشغيل جهاز. Debug preview وليست إصدار إنتاج بمفتاح المالك.

## ما تعطل وكيف عولج

run1: sdkmanager غير موجود في PATH؛ عولج بحل المسار من SDK الفعلي واختبارات الفشل. run2/3: طلب platforms;android-37 غير صحيح؛ قائمة الحزم أثبتت platforms;android-37.0، مع نشر جميع الاعتمادات المختارة. run4: assemble/JUnit نجحت لكن lint اكتشف قراءتي موارد غير متتبعتين للتكوين. عولجتا بـLocalResources/rememberUpdatedState وstringResource. run5 نجح دون تعطيل lint أو إنشاء baseline. الخطط في tasks/0002-* سبقت تعديلات التنفيذ.

## المجموعة المختبرة والحدود

Kotlin2.4.20، AGP9.3.1، Gradle9.6.1/JDK21، BOM2026.09.00، Activity1.13.0، Lifecycle2.11.0، Nav3 1.1.7، DataStore1.2.1، Coroutines1.11.0. compile/target37 وحزمة SDK37.0. لا ترقِّ الأرقام لمجرد تحذير دون فحص مصفوفة التوافق. لا SDK محلي؛ دليل Android من CI.

Wrapper مولد رسميًا ومرفق في artifact10525391589؛ لم تثبت ملفاته في Git بعد. أمر توليده في BUILD. تحذيرات الأيقونة والموارد وقواعد نقل البيانات وتحديث الأدوات مصنفة في TESTING. لا جهاز/محاكي أو screenshots أو قياس FPS/Baseline Profile حتى الآن.

## نقطة الاستئناف التالية — لا تقفز إلى حساب حقيقي

1. راجع معاينةAPK على جهاز/محاكي: الثيمات، الخط الكبير، RTL/English، التنقل والرجوع والضغط المتكرر وتغيير اللغة والتفضيلات. سجل الأدلة في TESTING، لا تفترض السلاسة من نجاح البناء.
2. خطط لتثبيت Wrapper المولد بعد التحقق من مصدره وبصمته، ثم عالج تحذيرات الأيقونة والنسخ الاحتياطي قبل أي جلسة حساسة.
3. اقرأ TELEGRAM_CAPABILITIES وSECURITY وDEPENDENCIES واكتب خطةP2 قبل native code: TDLib commit/JNI/schema متطابقة، credentials للتطبيق خارجGit، جلسة محمية قابلة للمحو، ثم دخول واختبار حساب بإذن المستخدم. لا تطلب أكواد أو توكنات في الوثائق أو المحادثة. لا Room مكرر لقاعدةTDLib أو WorkManager لاتصال حي أو Hilt دون حاجة.

المراجع: [PR1](https://github.com/ahmed9461/BotOS/pull/1) · [البناء الناجح](https://github.com/ahmed9461/BotOS/actions/runs/35288442618) · [الاختبارات والحدود](TESTING.md).
