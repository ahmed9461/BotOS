# ذاكرة BotOS — نقطة الاستئناف

آخر تحديث: **20 سبتمبر 2026**. الفرع: `feat/rich-chat-polish`. طلب الدمج: **5**.

**نسخة 0.4.0-preview الجانبية بُنيت وفُحصت ووُقعت، وهي جاهزة للتسليم في المحادثة. طلب التسليم معطّل الآن لمنع تكراره. لا تُنشئ مفتاحًا جديدًا ولا تُعد بناء النسخة بلا حاجة.**

## الخطوة التالية

انتظار تجربة المالك للنسخة الجديدة، وتسجيل ملاحظاته على رسائل Rich وشاشة المحادثة. لا توجد اختبارات معلقة لهذه النسخة المحددة، لكن القدرات المؤجلة أدناه ليست مكتملة.

قبل أي تعديل: اقرأ AGENTS وهذه الذاكرة، ثم البنية والخارطة والقرارات والسجل والأمان والقدرات والخطة والمصدر والاختبارات. افهم المطلوب، واكتب خطة قبل الكود، ثم نفذ واختبر واقرأ الأدلة ووثق. لا تتجاوز فشلًا ولا تعتبر اختبارًا مكتوبًا اختبارًا ناجحًا.

## الملف النهائي والأدلة

| العنصر | القيمة |
|---|---|
| المصدر المختبر | `84ff6ef85a5185f0fee0b955b8ff13009dbd5b33` |
| تشغيل التكامل | Run53 / `35526731984` |
| النتائج | 68 JVM، و8 native، و20 app، و56 Python؛ جميعها ناجحة |
| فحص الواجهة | 11 صورة، وفجوة الكيبورد 6.095238dp |
| lint | المحرك: صفر أخطاء وتحذيرات؛ التطبيق: صفر أخطاء و26 تحذيرًا |
| بناء المالك المهيأ | owner6 / `35527432601`، ناجح |
| الملف | `BotOS-0.4.0-preview.apk` |
| الحجم | 87,154,161 بايت |
| SHA256 للملف | `1ef4bbc9c224c508b8b09fc6950600d63d9d7a015788053bdac5453a0c47961a` |

توقيعا v2 وv3 تحققا بنجاح، ومحتوى ZIP غير التوقيعي يطابق APK المختبر قبل إعادة التوقيع. [الدليل الكامل](tasks/0006-owner-delivery-complete.md) و[الاختبارات](TESTING.md). التعديلات التي تلت المصدر أعلاه تخص الوثائق وطلب التسليم فقط.

## هوية النسخة والتصميم

بموافقة المالك، التطبيق الجديد `com.ahmed9461.botos.app` باسم **BotOS** يثبت بجانب 0.3 ذات الهوية `com.ahmed9461.botos.preview`. لا يحذف القديمة أو ينقل جلستها ومكتبتها ضمنيًا. يسجل المستخدم الدخول ويضيف أسماء البوتات في الجديدة.

هوية Porcelain / Graphite / Iris والأيقونة محفوظة. **مكتبتي والمظهر لا يعاد تصميمهما.** تعديل المحادثة يشمل Rich النصي، وصفوف الأزرار الخارجية، وفقاعات أصغر بوقت وحالة إرسال، ومبدّل البوتات بسهم. BotOsApp هو المالك الوحيد لمساحات النظام والكيبورد. مفاتيح WorkspaceStore ثابتة.

## المفتاح الدائم — لا تكرار خطأ 0.3

المفتاح الجديد RSA3072، وبصمة شهادة SHA256:

`6530ae8d20d9324504418f2afa274fec2cd1dc289ce3bdf2c8176f3620e7deba`

حزمة `BotOS-Private-Key-Backup-v1.zip` وملف `BotOS-Recovery-Key.txt` المنفصل محفوظان في Library الخاصة تحت BotOS Private، وسُلّمت روابطهما للمالك بالفعل. أُعيدت الحزمة من Library، واختُبرت استعادة PKCS12، ورفض الكلمة الخاطئة، وفك CMS، والتوقيع بالشهادة نفسها. **المفتاح المستعاد هو الذي وقّع ملف 0.4 النهائي.**

عند غياب ملفات بيئة العمل، استعد الملفين من Library ولا تنشئ مفتاحًا بديلًا. لا تضع كلمة الاستعادة أو معرفات Library الخاصة في Git. الشهادتان العامتان فقط داخل delivery. مفتاح 0.3 المفقود لم يتلقه المالك؛ لا تطلبه منه ثانية. راجع OWNER_DELIVERY قبل أي إصدار لاحق.

## الأمان والحدود

أسرار Telegram الموجودة لا يعاد طلبها. اختبارات PR بلا أسرار أو حساب مالك. حمولة المالك في GitHub مشفرة CMS، والتوقيع الدائم خارج CI. لا OTP أو كلمة مرور أو جلسة في الدردشة أو Git أو السجلات. بدء البوت صريح، والأفعال من البروتوكول لا التخمين، والمهلة لا تعيد إرسال الفعل.

مرفقات Rich بطاقات بيانات ووصف، وليست تنزيلًا أو تشغيلًا. الرياضيات صيغة نصية لا تنضيد كامل. بعض الرموز والمراجع بتمثيل مبسط. Streaming وMiniApps وPayments وقياسات الأداء الشاملة مؤجلة.

اختبار الاستبدال بشهادة CI يثبت بقاء علامات مصطنعة عند إعادة تثبيت APK نفسه؛ لا يثبت ترقية رقم إصدار أو اختبار الشهادة النهائية على جهاز المالك. المساعد لم يسجل الدخول بحساب المالك.

لا merge أو force-push. main يحتوي التخطيط؛ العمل الحالي على الفرع المذكور. لا جدولة ساعية فعالة ولا وعد بعمل مساعد خفي بعد الجلسة.
