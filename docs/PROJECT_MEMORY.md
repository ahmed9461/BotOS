# ذاكرة BotOS — نقطة الاستئناف

آخر تحديث: 2026-09-18. المهمة النشطة 0004 / P2a، الحالة validating. آخر خطة تصحيح: [0004-window-polling](tasks/0004-window-polling.md)، كتبت قبل الكود في8d25d289. لا تعتمد حالة قديمة من وصف PR أو ملخص محادثة.

## سير العمل الإلزامي

اقرأ AGENTS والذاكرة والبنية والخارطة والقرارات وخطة المهمة والكود والاختبارات قبل أي تعديل. افحص الفرع والـdiff. خطط ثم نفذ ثم اختبر ثم وثق؛ لا تتجاوز فشلًا ولا تعلن نجاح اختبار غير مقروء.

## التصميم والمنتج

اعتماد المالك الكامل للتصميم محفوظ في ADR-007 والمرجع158c5258. Porcelain / Graphite / Iris، الأيقونة الأصلية والتبويبات واللغة العربية RTL والثيمات ثابتة. لا تغيير جوهري دون موافقة، ولا مساس بمفاتيح DataStore وأسماء البوتات وترتيبها. BotOsApp المالك الوحيد لمساحات النظام والكيبورد؛ لا imePadding في composer. الحالة تجمع داخل NavEntry ولا يعاد إنشاء الشاشة قسرًا لتحديث المفتاح.

المنتج يجمع بوتات المستخدم ويفهم أنواع الرسائل والأفعال، دون AI يخمن callbacks أو تكامل منفصل لكل بوت. التطبيق ما زال معاينة0.2 محلية: لا علاقة app→core:tdlib أو تسجيل دخول أو إرسال حي حتى P2b. أسماء البوتات غير متحقق منها؛ فتح Telegram الخارجي ليس اتصالًا داخليًا. لا تجمع توكنات البوتات أو OTP أو كلمة مرور أو جلسات في Git/السجلات/المحادثة.

## الفروع والتاريخ

main عندca4776f5 يحتوي التخطيط فقط. feat/android-foundation/PR1 عند914ab136؛ feat/ui-refinement-v2/PR2 عند8aceb1a8. فرع العمل feat/telegram-runtime/PR3 بدأ من8aceb1a8. خطة09aaffca سبقت بناء native فيc0af78e8 وطبقة النقل. لا دمج فيmain ولا force-push؛ لا تبدأ منmain وتضيع التنفيذ الموجود.

## ما نفذ في P2a

TDLib1.8.67 مثبت عندd1085f9cebc5a62379991ae1652673954f229c1f، OpenSSL3.5.7LTS عند8cf17aaeb4599f8af87fefd810b5b5fee90fe69e، NDK29.0.14206865. بناء JSONJava الرسمي وABI arm64-v8a/x86_64 نجح في35372781224، وتحقق lock/schema/Java وبصمات المكتبات. تمييز محاذاةELF16KB عن اختبار جهاز16KB واجب.

core:telegram تضم TdRpc/TdTransport وترتيب updates وعزلclient_id وcorrelation والمهلة والإلغاء دون إعادة الفعل. Authorization تعتمد updates لا Ok لإثبات الدخول. core:tdlib تضم JSONJava الرسمي وAndroidTelegramSession بملكية استقبال حصرية ورمز يمنع إغلاق كائن قديم لعميل جديد. SessionVault يلف مفتاح القاعدة32بايت بـKeystore/AES-GCM داخل noBackupFilesDir؛ فقد المفتاح أو تلفه لا يمحو البيانات. close يحافظ على الجلسة؛ logOut يحتاج الإقرار والإغلاق قبل محو ملفات الجلسة فقط. لا app integration حتى الآن.

## نتائج التحقق المقروءة

0.2: تشغيل35294173820 على158c5258 ناجح؛ JVM4 وجهاز3 وسبع لقطات، gap6.095238dp، lint0أخطاء/18تحذيرًا. APK حجمه12,951,743بايت وبصمته5210194378a128239cf4baa882541b1110e6d7271c62db5a712b70a09d93cdb8. شهادةdebug dd6801c2da0c36e39b21d44a0079ed3c1660edf7bf3ab1f230dbe5324e219ff7 تختلف عن0.1؛ لا تطلب حذفًا يضيع بيانات المستخدم. WrapperJAR بصمته497c8c2a7e5031f6aa847f88104aa80a93532ec32ee17bdb8d1d2f67a194a9c7 طابقت المرجع الرسمي لكنه لم يثبت فيGit بعد.

P2a: قُرئت حزمة10561902603 من35377793103 على89351f43؛ SHA256 للأرشيفc3f9e92cf62db1149683ebc982866dbd38ccebfd9590e0a10a023da3b309aecb مطابق. XML تثبت21JVM و8JNI/Keystore ناجحة بلا فشل/تخطي. لكن UI توقف عند0/3 حتى مهلة8دقائق، لذلك التشغيل ككل فاشل. Android35377793037 فشل بالمهلة نفسها، وليس هذا عطلًا مثبتًا في تصميم الإنتاج.

التصحيح الحالي يزيل runOnIdle المتداخل مع getter النشاط، ويقرأ snapshot النافذة من scenario.onActivity على خيط الاختبار. لا احتفاظ بـActivity بعد recreation ولا تغيير لشروط القياس أو الست تبديلات أو اختبارات الإرسال/الاستعادة. يلزم تشغيل جديد قبل اعتماد النتيجة.

## أول خطوة تالية

اقرأ CI للـcommit التالي لخطة8d25d289: يجب نجاح21JVM و8JNI/Keystore و3UI وتجميع/lint؛ اقرأ native-runtime.txt وسبع لقطات وkeyboard-gap، ثم أغلق0004 بتحديث TESTING والخطط والسجل وPR. لا تعد إرسال APK0.2 كأنه ربطTelegram. بعد النجاح اكتب خطةP2b قبل الكود: الحساب والربط الحقيقي وحماية بيانات التطبيق وتوقيع ثابت واختبارات.

## الموانع الثابتة

بيانات تطبيق Telegram الخاصة بالمشروع غير مجهزة؛ تحفظ خارجGit، ولا تطلب OTP/كلمة المرور في المحادثة. تسجيل الدخول والبوتات والرسائل/الأزرار/edits/reconnect والوسائط/streaming لم تنفذ داخل app. مراجعةbackup والتوقيع الدائم وFPS/TalkBack والشبكة الضعيفة والأجهزة المتعددة/16KB ما زالت بوابات مستقلة. JDK21/Kotlin2.4.20/AGP9.3.1/Gradle9.6.1/BOM2026.09.00/Nav3 1.1.7/DataStore1.2.1 كما فيDEPENDENCIES؛ platform37.0، المحاكي35، min26. البيئة المحلية بلاAndroidSDK؛ مصادر العمل من أرشيفCI والتحقق عبرActions. لا استمرار تلقائي للتطوير بعد انتهاء الرد.
