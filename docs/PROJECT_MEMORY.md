# ذاكرة BotOS — نقطة الاستئناف

آخر تحديث: 2026-09-19. الفرع feat/rich-chat-polish / PR5. المهمة0006، وتصحيحها النشط [0006-validation-repair](tasks/0006-validation-repair.md) فوق 0a411ed1. اقرأ AGENTS والبنية والخارطة والقرارات والسجل والأمان والقدرات والمصدر والاختبارات قبل التنفيذ؛ خطط ثم نفذ واختبر واقرأ الأدلة ووثق.

## الثوابت

Porcelain / Graphite / Iris والأيقونة معتمدة. المالك طلب تعديل شاشة المحادثة فقط: Rich، أزرار خارج الفقاعة، فقاعات صغيرة بوقت، ومبدّل بسهم بدل تبويبات ضخمة. مكتبتي والمظهر ممتازان ولا يعاد تصميمهما. لا تغيير لمفاتيح WorkspaceStore أو أسماء البوتات وترتيبها أو الجلسة. BotOsApp وحده يملك مساحات IME/systemBars/displayCutout.

## ما يعمل وما كتبه المنفذ

نسخة0.3 سُلّمت وجرب المالك حسابه وبوتاته بنفسه وأكد عمل الوظائف الأساسية. TDLib d1085f9c وJSONJava مثبتان. AccountCoordinator وSessionVault/Keystore يعملان؛ لا حساب مالك في CI. فصل الحساب والمحادثة والجيل ومراجعة الأزرار ثابت؛ /start صريح ولا يعاد عند التبديل، والمهلة لا تعيد الفعل.

في0006 كُتب StyledText وRich blocks ومحول messageRichMessage وعرضها، وفصل صفوف الأزرار والوقت والمبدّل والرأس المضغوط، وخمس حالات محول وحالتا واجهة جديدتان. لكن هذه تعديلات غير مجتازة بعد. لا تكرر مرحلة إنشائها من الصفر؛ راجع تنفيذها وصححه. لا تزال بعض العائلات تمثل كنص/metadata فقط وليست كل ميزات Telegram.

## آخر بوابة

Run46 /35455984545 عند0a411ed1: native arm64-v8a وx86_64 نجحا. التحقق توقف قبل تجميع Kotlin واختبارات الجهاز بسبب غياب تحديث CHANGELOG. سجل job105933959708 قرئ؛ حزمة10588438470 طابقت SHA2560ecdeb47449c88dbb631d6c4bc7da7fa7e44d22286ed12ae57703166044ffd22، وفيها المصدر فقط. لا يقال إن الاختبارات الـ16 نجحت.

آخر دليل قديم مكتمل0.3: Android35401850435 وowner35401845819. لا يثبتان كود0006. المخطط المتاح بنفس الإصدار يحوي getFullRichMessage ويحدد الحاجة إليه عند is_full=false. راجع البنية الفعلية لا أسماء Bot API المتشابهة.

## الشريحة التالية

اتبع0006-validation-repair: أكمل التوثيق، صحح حدود التحويل والأزرار والتفاعل وRich الجزئي ومحاذاة RTL، ثم اختبر القراءة والعرض والمبدّل والكيبورد وراجع لقطات محادثة غنية فعلية من fixtures لا حساب المالك. المكتبة والمظهر والأيقونة بلا تغييرات. حدّث الدليل والخطة بعد كل نتيجة. لا إعادة محاولة عمياء أو تخفيف شروط.

## الأسرار والتسليم

BOTOS_TELEGRAM_API_ID وBOTOS_TELEGRAM_API_HASH تحقق وجودهما سابقًا، ولا يعاد طلبهما. PR CI بلا أسرار. لا OTP/password/session أو api_hash في Git/logs/المحادثة. مسار owner-delivery الحالي مقيد بفرع0.3؛ يجب تكييفه بعد نجاح المصدر الجديد لا قبله، وتبقى الحمولة مشفرة.

مفتاح توقيع0.3 وشهادة فك التغليف الخاصة غير ظاهرين في الملفات المركبة الحالية. توجد APK السابقة وحمولة CI مفكوكة فقط، لا keystore. الذاكرة القديمة تقول إن نسخة احتياطية جهزت لكن لم يظهر دليل تسليم ملفها. لا تفترض إمكانية ترقية بنفس الشهادة ولا تغير الشهادة أو تطلب حذف بيانات المالك بصمت. أبلغ المالك بالمانع إن تعذر استردادها.

## الحدود

لا ادعاء تنزيل/تشغيل الوسائط أو تنضيد رياضي كامل أو streaming/MiniApps/Payments من بطاقات metadata. قياس FPS وTalkBack والشبكة الضعيفة والأجهزة المتعددة وruntime16KB مستقل. المصدر المحلي منartifact46؛ الحاوية بلا اتصال شبكة أو AndroidSDK، وCI بيئة التحقق الأساسية. main تخطيط فقط؛ لا merge أو force-push. المالك يفضل المتابعة داخل الجلسة، والمهمة الساعية السابقة متوقفة؛ لا وعد بعمل بعد نهاية الجلسة دون أداة جدولة.
