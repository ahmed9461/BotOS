# ذاكرة BotOS — نقطة الاستئناف

آخر تحديث: 2026-09-18. المهمة النشطة: [0005 — ربط الحساب والبوتات](tasks/0005-account-connection.md). الشريحة الحالية: [تثبيت بيئة UI بعد تشخيص Launcher ANR](tasks/0005-emulator-focus.md). خطتها في parent `072e5ddc` فوق `e8f86b4c` على `feat/telegram-connection` / PR4؛ تعديل harness التالي لا يغير واجهة الإنتاج أو عتبات الاختبار.

## قراءة إلزامية

AGENTS وهذه الذاكرة والبنية والخارطة والقرارات والأمان والقدرات وخطة المهمة، ثم المصدر والاختبارات. افحص أحدث الفرع وCI وفرق التغييرات، ثم افهم وخطط ونفذ واختبر ووثق بالترتيب. لا تبدأ من main أو تستبدل حالة المستودع بذاكرة المحادثة. لا تختزل الاختبار إلى وجود ملف أو workflow.

## التصميم والبيانات

اعتمد المالك التصميم Porcelain / Graphite / Iris والأيقونة في ADR-007 عند 158c5258. لا تغيير جوهري دون موافقة. حافظ على الألوان والثيمات والتبويبات وRTL وأسماء البوتات وترتيبها ومفاتيح DataStore. الشاشات الجديدة تستخدم النظام نفسه.

BotOsApp وحده يملك اتحاد مساحات النظام والكيبورد؛ لا imePadding إضافي في composer. تجمع الوجهات حالتها داخل NavEntry، لا إعادة إنشاء قسرية لإظهار toggle. لا AI يتخمن callbacks ولا /start عند كل تنقل.

## حالة بيانات التطبيق

أكد المالك في 2026-09-18 إضافة BOTOS_TELEGRAM_API_ID وBOTOS_TELEGRAM_API_HASH إلى GitHub Secrets. **تم التحقق منها فعليًا في التشغيل الموثوق 35387386142 على e8f86b4c دون كشف القيم:** وصل الزوج كاملًا إلى المهمة السرية، اجتاز تحقق الصيغة، وتطابق BuildConfig المولد مع المدخلات داخل runner. خرج التحقق فقط بحالة configured، والمصادقة بقيت not_tested. لا تطلب إعادة إضافة الأسرار أو إرسالها في المحادثة دون مانع جديد مثبت.

التحقق الموثوق مستقل عن PR CI؛ لا تمرر الأسرار إلى fork أو pull_request_target. المهمة السرية عطلت cache وbuild scan ولم ترفع APK أو BuildConfig المولد. بيانات العميل داخل APK لاحقًا ليست غير قابلة للاستخراج. نجاح الصيغة والتوليد لا يعني أن Telegram قبل الحساب أو أن الدخول جُرّب.

## المنفذ فعليًا

P2a مكتملة كمحرك مستقل: TDLib/JSONJava مثبتان، TdRpc/TdTransport لترتيب التحديثات وربط الطلب بجوابه وعزل client_id والمهلة والإلغاء دون إعادة الفعل، وAuthorization من التحديثات لا Ok. AndroidTelegramSession بمالك استقبال حصري، وSessionVault يحمي مفتاح القاعدة عبر Keystore/AES-GCM. close يحفظ الملفات؛ logOut يمحوها بعد الإقرار والإغلاق. فقد المفتاح لا يمحو البيانات ضمنيًا.

P2b الأولى: AccountSession/Factory وConnectionPreferences وAccountCoordinator. موافقة وتهيئة قبل الفتح، مراحل من TDLib وgetMe بعد READY، جيل للجلسة يمنع الرد القديم من تغيير حساب جديد، وإذن استعادة متسلسل لا يعاد تفعيله بعد طلب الخروج. الإلغاء والخروج وفشل التنظيف حالات مختلفة. المدخلات الحساسة لا تدخل حالة العرض أو التشخيص.

تهيئة البناء: public BuildConfig API في AGP يختار زوج البيئة كاملًا أو local.properties المستبعد من Git، ويرفض الجزئي/غير الصالح دون طباعة قيمة أو طول أو بصمة. 24 اختبار Python تشمل 11 حالة خاصة بهذه التهيئة نجحت في التشغيل الموثوق وفي PR CI.

**app ما زال معاينة 0.2 محلية؛ لا تسجيل دخول أو إرسال حي داخله بعد.** حفظ اسم بوت ليس تحقق هوية، وفتح تطبيق Telegram الخارجي ليس ربطًا. لا ترسل المعاينة القديمة بوصفها النسخة المتصلة المنتظرة.

## أحدث تحقق مقروء

التشغيل الموثوق 35387386142 على e8f86b4c: تحقق الأسرار والتوليد نجح كما هو موضح أعلاه، بلا اتصال بحساب.

Android 35387390277 على e8f86b4c: فحوص المستودع والتباين والعقد و24 Python وJVM/lint/assemble نجحت، ثم فشلت الحالات UI الثلاث كلها في `UiRegressionTest.windowReady` بعد مهلة 10 ثوانٍ. native 35387390278: بناء arm64-v8a وx86_64، العقد، JVM و8 JNI/Keystore نجحت، ثم فشلت الحالات UI الثلاث عند الحاجز نفسه. لا يُعاد تصنيفهما كنجاح كامل.

حزمة native `BotOS-runtime-checks-12` فُتحت. لقطة `failure-screen.png` تعرض dialog نظام `Pixel Launcher isn't responding`. `dumpsys window lastanr` يحدد `com.google.android.apps.nexuslauncher/.NexusLauncherActivity` والسبب `Input dispatching timed out (Application does not have a focused window)`، و`mCurrentFocus` هو dialog الـANR. `dumpsys power` يثبت أن الجهاز Awake. إذًا الدليل الحالي يحدد فشل بيئة HOME قبل أن تملك BotOS التركيز، لا عطبًا مثبتًا في UI نفسه.

خطة 0005-emulator-focus سبقت التعديل: إزالة KEYCODE_MENU من التهيئة، حل HOME ديناميكيًا وإيقافه على المحاكي غير الآمن فقط، وضع Settings النظامية في المقدمة والتحقق من resumed activity بمهلة تجهيز مستقلة. تبقى `windowReady=10s` وIME=15s وgap=-2..12dp كما هي. نتائج CI لهذا التعديل لم تُفترض بعد.

## أدلة سابقة

c016f00a أصلح توقيع JUnit دون تغيير assertions. نجح تشغيل المحرك 35384252317: 36 JVM، منها 15 لمنسق الحساب، و8 JNI/Keystore و3 UI بلا فشل أو تخطي. قُرئت XML وnative-runtime والقياس من الحزمة 10563976822. lint المحرك 0/0؛ الجهاز أعاد الإصدار والمصدر الصحيحين والعربية/emoji دون حساب؛ gap=6.095238dp. Android المستقل 35384252163 كان قد فشل أيضًا في انتظار تركيز النافذة؛ النجاح الموازي لم يمح ذلك.

P2a على e9b45c60 اجتازت 35380280277 و35380280304: 21 JVM و8 JNI/Keystore و3 UI، وlint المحرك 0/0، وملفات الجهاز واللقطات والقياس وAAR. انظر tasks/0004-validation-complete.md وTESTING. لا حساب مستخدم استُخدم.

النسخة 0.2 الأولى على 158c5258 اجتازت 35294173820: سبع لقطات وgap=6.095238dp. SHA256 APK=5210194378a128239cf4baa882541b1110e6d7271c62db5a712b70a09d93cdb8، وحجمه 12,951,743 بايت. شهادة debug تختلف عن 0.1؛ لا وعد بتحديث فوقها ولا حذف صامت للبيانات.

## الفروع والاعتمادات

main عند ca4776f5 تخطيط فقط. PR1/feat/android-foundation ثم PR2/feat/ui-refinement-v2 ثم PR3/feat/telegram-runtime عند 191c0466. PR4/feat/telegram-connection فوقها. لا دمج أو force-push. المجموعة المثبتة في DEPENDENCIES وnative/dependencies.lock.json كما هي؛ TDLib d1085f9cebc5a62379991ae1652673954f229c1f مع schema/Java/JNI متطابقة. لا ترقية دون خطة ومصدر واختبار.

## أول خطوة تالية

شغّل بوابتي Android وnative على تعديل harness المخطط واقرأ XML واللقطات وتشخيص التركيز. لا تكرر تشغيل المصدر الفاشل عشوائيًا. إذا نجحت الحالات الثلاث مع البوابات الأخرى، أغلق ملحق build-configuration بالأدلة ثم انتقل لشاشة الحساب وموافقة المستخدم وحفظ إذن الاستعادة في ملف منفصل، وربطها بمنسق واحد وTDLib الحقيقي. لا تفتح نموذج هاتف/كود قبل اكتمال هذا الربط.

## حدود التسليم

لا OTP أو كلمة مرور أو جلسة أو api_hash في Git أو logs أو المحادثة. لا مفاتيح عينة أو تطبيق آخر. تجربة حساب المالك تتم داخله بنفسه وبموافقته فقط. التوقيع الدائم وسياسات backup والنشر والأداء/TalkBack والشبكة الضعيفة والأجهزة المتعددة وruntime16KB ووسائط/Rich/streaming/MiniApps لم تكتمل. البيئة المحلية بلا Android SDK؛ التحقق عبر Actions. المستخدم ينتظر APK عند الجاهزية، لا AAR أو معاينة قديمة.
