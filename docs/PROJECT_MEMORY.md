# ذاكرة BotOS — نقطة الاستئناف

آخر تحديث: 2026-09-18. العمل في `feat/telegram-connection` / PR4 فوق PR3. المهمة0005 لربط الحساب والبوتات. **شاشة الحساب اجتازت التشغيل29؛ تصحيح نطاق I/O للمخزن ينتظر التحقق التالي.** اقرأ [دليل الحساب](tasks/0005-account-ui-evidence.md) و[خطة تصحيح I/O](tasks/0005-account-io-review.md). لا تعد تنفيذ الحساب من البداية.

## قبل العمل

اقرأ AGENTS والذاكرة والبنية والخارطة والقرارات والأمان والقدرات والخطة، ثم الكود والاختبارات. افحص أحدث الفرع وCI والـdiff، افهم وخطط قبل التعديل، نفذ واختبر واقرأ الأدلة ووثق بعدها. لا تخفف اختبارًا أو تكرر فشلًا عشوائيًا. المالك طلب متابعة البناء داخل الجلسة؛ أوقفت المهمة الساعية. لا تنفيذ خلفي متزامن ولا وعد باستمرار تلقائي بعد إنهاء الجلسة.

## التصميم والبيانات

Porcelain / Graphite / Iris والتبويبات والثيمات وRTL والأيقونة الأصلية معتمدة فيADR-007 عند158c5258. لا تغيير جوهري دون موافقة جديدة. أسماء البوتات وترتيبها ومفاتيحWorkspaceStore محفوظة. BotOsApp وحده يملك union لمساحات النظام والكيبورد؛ لاimePadding فيالشاشات. الحالة تجمع داخلNavEntry؛ لا إعادةإنشاءقسرية لإظهارtoggle. لاAI يتخمنcallbacks ولا/start عندكلتبديل.

## ما يعمل وما لم يكتمل

P2a محركTDLib مستقل مختبر: JSONJava أصلي، ربطالطلبات والأجوبة وعزلclient_id وترتيبupdates والإلغاء/المهلة دونإعادةالفعل. SessionVault يحمي مفتاحالقاعدة عبرKeystore/AES-GCM؛ close يحفظالجلسة وlogOut يمحوهابعدالإقراروالإغلاق. تلفالمفتاح يفشلصراحةدونمسحضمني.

AccountCoordinator مختبر بموافقة قبلالفتح، وgetMe بعدREADY، وعزلأجيالالجلسة وإذنالاستعادة. BotOsApplication يملكمنسقًاواحدًا ويفتحJNI خارجالرسم؛ AccountRestoreStore ملفمنفصل فيnoBackupFilesDir. AccountRoute/Screen مرتبطتانبه فعليًا: تهيئة/موافقة/هاتف/كود/كلمةمرور/بريد حسبحالاتTDLib، ومدخلاتغيرمحفوظة وFLAG_SECURE أثناءالإدخال وخروجبالتأكيد. نسخةغيرمهيأة لاتعرضحقولدخول. Manifest يمنعbackup وdevice-transfer. [تفاصيل الربط](ACCOUNT_INTEGRATION.md).

**لم يُستخدم حساب Telegram حقيقي بعد، ولم يكتمل عرض محادثات البوتات ورسائلها وأزرارها الحية.** المعاينة باقية منفصلة. لا ترسلAPKغيرمهيأ أوAAR أو0.2القديمة بوصفه التطبيق المتصل. الإصدار الحالي0.3.0-account/code3، وليس إغلاقًا لكاملP2b.

## أحدث دليل

fe7604f3 اجتاز35392287279: قرئت حزمة10566705700 المطابقة لـSHA256cd2be4d4b20203037daccfc07df893e41ea5afbf127022b247e34a28ba69ffcb. 36JVM و8JNI/Keystore و10app بلا فشل/تخطي. lintالمحرك0/0، التطبيق0أخطاء/20تحذيرًا. ثماني لقطات فُتحت، gap6.095238dp، وnative-runtime أثبت1.8.67 وd1085f9c والعربية/emoji وaccountUsed=false. دليل الحساب يذكر الحدود؛ الاختبارات المصطنعة ليست مصادقة بخوادمTelegram.

مراجعة المصدر لاحظت وراثةMain فيscopeالمخزن. خطة8ac448ae سبقت تغييره إلىCoroutineScope(scope.coroutineContext + Dispatchers.IO) مع الحفاظعلىJob، وإضافة حالةجهاز ثالثة للمخزن تُستدعىمنMain. البوابةالتالية36JVM +8native +11app، و8صور وgap وlint/build. **الحالة الجديدة لم تُعتبر ناجحة قبل تشغيلها.**

## الأسرار والبناء

أضاف المالك BOTOS_TELEGRAM_API_ID وBOTOS_TELEGRAM_API_HASH؛ تحقق الوصول والصيغة وتطابقBuildConfig في35387386142 علىe8f86b4c دونكشفها. لا إعادةطلبهما. configured ليسقبولTelegram. الأسرار فيworkflowدفعموثوق فقط؛ لاPR CI أوpull_request_target أوcache/buildscan/مصادر مولدة فيartifacts. بياناتالعميلداخلAPK قابلةللاستخراج، فلا تدععكسذلك.

Androidfoundation يستدعيtdlib-native كـworkflow_call مننفسالمصدر دونinheritsecrets. nativeمعماريتان ثمJVM/lint/build ثممحاكيواحد لكلالحالاتالقديمةوالجديدة؛ لا تكرارمحاكياتلنفسالتعديل. PRيبنيAPKغيرمهيأ للاختبارات. مسارتسليممهيأ لم يكتملبعد. لاحساب مستخدمفيCI.

## الاستئناف بالترتيب

1. اقرأ نتيجة تصحيحI/O وجميعXML والصور والأدلة؛ عالجالفشلمنسببه ووثق قبلتجاوزه.
2. بعد نجاح الحساب، خطط لشريحة البوتات منخطوة5 في0005-account-connection: userTypeBot، نصوص، Inline/Reply، تعديلات، حالاتالإرسال وعزلالحساب/المحادثة، وفقschemaالمثبت.
3. اختبر الشريحة ثم جهزAPKمهيأ فيمسارموثوق معفحصبصمةوتوقيع وتسليمصريح بالحدود. لا تعتبروجودشاشةدخول إكمالًالمحادثاتالبوتات.

## المراجع والتاريخ

mainتخطيطca4776f5؛ PR1/feat/android-foundation914ab136 → PR2/feat/ui-refinement-v2 → PR3/feat/telegram-runtime191c0466 → PR4. لا دمج أوforce-push أوالبدءمنmain. الاعتمادات وnative/dependencies.lock.json مثبتة؛ TDLibd1085f9cebc5a62379991ae1652673954f229c1f معschema/Java/JNIمطابقة. لا ترقيةدونخطةومصدرواختبار.

ca668e00 اجتاز35389683160 و35389683150 بعدتشخيصPixelLauncher ANR خارجي؛ لمتتغيرمهلfocus10s/IME15s/gap-2..12dp. c016f00a اختبرمنسقالحساب36/8/3 في35384252317. P2a قبلهاe9b45c60 في35380280277؛ راجع0004-validation-complete وTESTING. نجاحلاحقلايمحوفشلًاقديمًا.

## حدود الأمان والتسليم

لاOTP/password/session/api_hash فيGit أوlogs أوالمحادثة أوSavedState/backup. دخولحسابالمالك يتمبيده داخلالتطبيق فقط. لا مفاتيحعينةلاتصالحي. شهادةdebug0.2 اختلفتعن0.1؛ لا حذفصامت للبيانات. التوقيعالدائم، وتجربةحسابالمستخدم، والنشر، والأداء/TalkBack/الشبكةالضعيفة/أجهزةمتعددة/runtime16KB، وRich/وسائط/streaming/MiniApps لمتكتمل. Wrapperفُحص وCIيولدهرسميًا. البناءعبرActions لأنالبيئةالمحليةبلاأندرويدSDK.
