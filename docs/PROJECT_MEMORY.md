# ذاكرة BotOS — نقطة الاستئناف

آخر تحديث: **22 سبتمبر 2026**. الفرع النشط: `feat/media-profiles-streaming`، فوق `86518e8cd7a44b6f6825bd3037d3d4559cb70776`. المهمة النشطة: [0007 — صور البوتات والوسائط والبث](tasks/0007-media-profiles-streaming.md). [سجل التقدم](tasks/0007-progress.md).

## المطلوب واعتماد المالك

جرب المالك 0.4 وقيّمها 90%. يريد صورة Telegram أو صورة مخصصة بدل الحرف في المكتبة ورأس المحادثة والمبدّل، واستلام الصور والفيديو والملصقات، وإرسال الصور والفيديو والصوت من الهاتف، وبث ردود الذكاء الاصطناعي ومراجعة جميع عائلات Rich. هذا قبول من المستخدم وليس قياس أداء اختباريًا.

التصميم والأيقونة والألوان ثابتة. لا تعاد المكتبة والمظهر إلى التصميم من البداية؛ إضافة موضع الصورة وإجراءات تخصيصها فقط مسموحة ضمن الطلب.

## قبل التنفيذ

اقرأ AGENTS والذاكرة والبنية والخارطة والقرارات والسجل والأمان والقدرات ودليل الواجهة والخطة والمصدر والاختبارات. افهم وحدود الوحدات ثم خطط ونفذ واختبر واقرأ الدليل ووثق. لا تتجاوز فشلًا أو تعيد اختبارًا عشوائيًا حتى ينجح. لا force-push أو merge.

## المنفذ في الشريحة الأولى من0007

الخطة `be4cf4ef` سبقت الكود. أضيف محول الوسائط العادية ومنشئ مدخلات المرفقات، وخدمة TelegramFiles لتنزيل معزول بالحساب/الجيل وتوازٍ ثلاثة، وخدمة BotProfiles للأسماء المحفوظة بعد التحقق من البوت. أضيف PendingReplies وربطه بمنسق المحادثة: محتوى مؤقت متدفق، مدة رسمية، توقف، رفض الموضوع الأجنبي والأفعال قبل الرسالة النهائية.

20 اختبار JVM جديدًا مكتوبًا؛ المتوقع88JVM مع20app و8native السابقة. **هذه الشريحة تنتظر أول CI؛ كتابة الاختبارات أو فحص تركيب Kotlin محليًا لا يعني نجاحها.** لم تُربط الواجهات بالصور أو المشغلات أو الإرسال بعد. لا تسلّم ملفًا على أنه ينفذ جميع طلبات المالك.

## أول خطوة تالية

افحص CI للشريحة الحالية واقرأ XML. عند نجاحها، اربط BotProfiles/TelegramFiles بالتطبيق، وخزن الصورة المخصصة من PhotoPicker خارج Main، ومررها إلى شارات المكتبة والرأس والمبدّل. ثم اعرض pending داخل قائمة المحادثة دون تخزينه كتاريخ. تليها مشغلات الوسائط والملصقات وواجهة إرسال/معاينة/إلغاء المرفقات مع اختبارات عزل الهدف والتنظيف.

## المرجع المسلم السابق

APK0.4 من `84ff6ef8`، وCI53/35526731984:68JVM+8native+20app+56Python و11لقطة؛ فجوة IME6.095238dp. owner6/35527432601 ناجح. آخر docs-only CI55/35528198980 على86518e8c ناجح. لا يُنسب نجاحهما إلى0007. تفاصيل النسخة السابقة في tasks/0006-owner-delivery-complete.md.

## ثوابت الحساب والأمان

TDLib/JSONJava/JNI عندd1085f9c. البث هو updatePendingMessage وupdateStopMessageDraft، لا updateChatDraftMessage. مدة العرض من pending_text_message_period. /start صريح؛ callback لا يُخمّن؛ المهلة لا تعيد الفعل. لا OTP أو كلمات مرور أو جلسات أو مرفقات حقيقية في Git أو السجلات. PR CI بلا أسرار. أسرار Telegram الموجودة لا يعاد طلبها.

BotOsApp وحده يملك Insets. مفاتيح WorkspaceStore والأسماء والترتيب والجلسة ثابتة. لا قراءة مسارات من نص البوت، ولا خادم مركزي للوسائط، ولا إرسال من منتقي الملفات قبل تأكيد المستخدم.

## المفتاح الدائم وهوية التثبيت

هوية التطبيق **com.ahmed9461.botos.app** لا تتغير. بصمة الشهادة:
`6530ae8d20d9324504418f2afa274fec2cd1dc289ce3bdf2c8176f3620e7deba`.

حزمة BotOS-Private-Key-Backup-v1.zip وملف BotOS-Recovery-Key.txt منفصلان، محفوظان في Library الخاصة BotOS Private وسُلّمت روابطهما. عند غياب الملفات المركبة استعدهما من Library، ولا تنشئ مفتاحًا جديدًا. لا تطبع كلمة الاستعادة أو تضع معرفات Library الخاصة في Git. راجع OWNER_DELIVERY قبل إصدار تالٍ.

## الحدود الحالية

واجهات0.4 ما زالت بطاقات metadata للوسائط، والبث الجديد في النواة فقط إلى حين ربطه واختباره. الرياضيات وبعض المراجع والرموز مبسطة. يلزم مصفوفة لكل عائلة Rich من المخطط مع دليل وحدود؛ MiniApps/Payments/LoginURL ليست مكتملة ولا تفعل تخمينًا. لا ادعاء فحوص FPS/TalkBack/runtime16KB الشاملة. الجدولة الساعية غير فعالة، والتنفيذ في جلسة العمل الحالية.
