# ذاكرة BotOS — نقطة الاستئناف

آخر تحديث: 2026-09-18. المهمة النشطة: [0003-ui-refinement](tasks/0003-ui-refinement.md)، in_progress. **اقرأ AGENTS وهذه الذاكرة والبنية والخارطة والخطة والكود قبل أي تعديل. خطط ثم نفذ ثم اختبر ووثّق؛ لا تتجاوز المرحلة ولا تعد إنشاء المنجز.**

## المنتج والثوابت

Android لبوتات Telegram المختارة في تبويبات قابلة للتسمية والترتيب؛ renderer عام للأنواع المدعومة بلا AI يخمن callbacks ولا تكامل منفصل لكل بوت. العربية/RTL وEnglish/LTR وDark/Light/System. لا نصوص هندسية في UI ولا كحلي أو أخضر كهوية. التطبيق الحالي معاينة محلية صريحة، بلا TDLib/JNI/login أو إرسال حي.

## مكان الوصول

main: تخطيطP0 عندca4776f5. الأساس في feat/android-foundation / PR1، رأسه914ab136 وقت الاستئناف. فرع المهمة الحالية feat/ui-refinement-v2 مشتق منه؛ لا تعِد التأسيس من main ولا تمس مستودعات أخرى.

## المنجز والمثبت قبل الجولة الثانية

خمس وحدات app/model/telegram/data/designsystem، Compose/Nav3 وDataStore للأسماء والترتيب والإعدادات، نماذج بعزل account/chat/revision وrenderer ومعاينة مستقلة. أسماء البوتات bookmarks غير متحقق منها. فتح الرابط خارجيًا ليس اتصال BotOS حيًا.

Run35288442618 نجح على7724a5f: JUnit4 (إحداها تجمع37 assertion)، Python13، contrast14، lint0 errors/9 warnings، assembleDebug ورفعAPK/Wrapper. APK0.1 حجمه12,910,987 بايت وSHA256 b33a1093f52d0d6bb49ecfac88e05284d7b604cc3caa9c75330bf92fe526516b. الأدلة فيTESTING والتاريخ محفوظ فيGit.

عولجت سابقًا مشكلة PATH لـsdkmanager، وحزمةSDK هي platforms;android-37.0 لا android-37، وموارد اللغة عبرLocalResources/stringResource. المجموعة المختبرة Kotlin2.4.20/AGP9.3.1/Gradle9.6.1/JDK21/BOM2026.09.00/Nav3 1.1.7/DataStore1.2.1؛ لا ترقيات عشوائية. Wrapper مولد رسميًا فيartifact10525391589 ولم يثبت فيGit. البيئة المحلية بلاAndroidSDK والوصول المباشر للشبكة فشل؛ Android يبنى فيCI.

## مراجعة المالك الجديدة — ملزمة

جرّب APK وأرسل صورًا: فراغ كبير بينIME وحقل الكتابة؛ تقليل الحركة لا ينعكس حتى الخروج والعودة؛ الألوان ثقيلة وغير مريحة؛ الواجهات تحتاج تحسينًا حقيقيًا مع الإبقاء على فكرة المكتبة/التبويبات. **أيقونة التطبيق أعجبته جدًا: لا تعدل ملفاتها أو لون خلفيتها.** وافق على جولة ثانية.

## المهمة الحالية

الخطة0003 كتبت قبل الكود. يبدأ الإصلاح بتوحيدinsets وجمع الحالة داخل الوجهة، ثم إعادة التصميم بألوان محايدة أوضح ولمسة بنفسجي خفيفة دون أسطح بنية واسعة. لا تغيير مفاتيحDataStore ولا مسح بيانات. يضاف اختبار جهاز آلي للكيبورد/switch والثيم والحفظ وصور حقيقية. لا تدّع حلًا مختبرًا قبل قراءة النتائج.

## الخطوة التالية الدقيقة

نفذ خطوة2 من خطة0003 ثم3؛ ابن الواجهات بعدهما؛ أضف اختباراتandroidTest وworkflow محاكي وراجع النتائج والصور. ارفعAPK0.2 فقط مع وصف حدود التحقق. احتفظ بـlauncher دون فرق عن914ab136. حدّث هذه النقطة عند التوقف.

## ما بعد ذلك

مراجعة جهاز المالك للألوان وIME، ثم تثبيتWrapper ومراجعة التحذيرات/backup، ثم خطةP2 لTDLib علىcommit/JNI/schema متطابقة وحمايةKeystore ومحو الجلسة وبيانات التطبيق خارجGit وحساب تجريبي بإذن المالك. لا أكواد دخول أو توكنات في المحادثة/الملفات. لا Room يكررTDLib ولاWorkManager لاتصال حي. FPS/TalkBack/شبكة ضعيفة ونشر إنتاجي ما زالت بوابات منفصلة.
