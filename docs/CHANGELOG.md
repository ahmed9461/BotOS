# سجل التغييرات

## 2026-09-18 — الجولة الثانية 0.2، التحقق جارٍ

خطة 0003 سبقت التنفيذ، وفتح فرع `feat/ui-refinement-v2` فوق `914ab136` وطلب الدمج رقم 2. توحيد معالجة مساحات النظام والكيبورد، وجمع الحالة داخل وجهات التنقل، وإعادة تصميم الألوان والمحرر والرسائل والثيمات، وإضافة مكتبة محلية قابلة للبحث. أصول الأيقونة ومفاتيح التخزين لم تتغير.

أضيفت ثلاثة اختبارات تشغيل Compose على محاكي API 35، مع حفظ الصور وقياس الفراغ، وفحص ثبات الأيقونة وتطابق الترجمة. لا يفترض نجاح هذه التعديلات من اختبارات النسخة الأولى، ولا تتضمن هذه الجولة اتصال Telegram حيًا.

## 2026-09-18 — تسليم المعاينة الأولى

نجح التشغيل `35288442618` على `7724a5f`: 13 اختبار Python، و14 فحص تباين، وأربع حالات JUnit، وlint دون أخطاء ومع تسعة تحذيرات، وبناء APK. حجم الملف 12,910,987 بايت، وبصمته `b33a1093f52d0d6bb49ecfac88e05284d7b604cc3caa9c75330bf92fe526516b`. رفعت حزم التطبيق والأدلة وWrapper. كان `914ab136` تحديث توثيق فقط بعد الكود المختبر.

## 2026-09-18 — إصلاحات البناء الأول

فشلت المحاولة الأولى لأن sdkmanager خارج PATH؛ عولج مساره مع اختبارات الفشل. أثبتت قائمة الحزم في المحاولتين الثانية والثالثة أن الاسم الصحيح `platforms;android-37.0`، وأكدت بيانات النشر الاعتمادات المختارة. نجح التجميع وJUnit في المحاولة الرابعة، لكن lint اكتشف قراءتين غير متتبعتين لموارد اللغة. كتبت الخطة في `e820da62` قبل إصلاح `LocalResources/rememberUpdatedState` و`stringResource` في `7724a5f`. نجحت المحاولة الخامسة دون تعطيل lint أو استخدام baseline.

## 2026-09-18 — الأساس والتخطيط

P0: README وAGENTS والذاكرة والبنية والخارطة والتصميم وقواعد العمل والأمان والقدرات والإصدارات وخطة 0001، قبل كتابة التطبيق. كان main عند `ca4776f5`.

P1: خمس وحدات Kotlin/Compose وDataStore وNavigation 3 ومحرك معاينة محلية وثيمات وRTL/English وأيقونة أصلية. أول تنفيذ في `0ac337e6`. ضمت اختبارات النواة 37 تحققًا، ثم شغلت ضمن حالة JUnit جامعة مع ثلاث حالات للمعاينة. لا TDLib أو تسجيل دخول أو وسائط حية في هذه المرحلة.
