# 0006 — إغلاق التحقق وتصحيح العرض قبل التسليم

2026-09-19. خطة قبل التصحيح، فوق 0a411ed124245b9cd81f371e3bb0cc2d7c25693e في feat/rich-chat-polish / PR5.

## الدليل المقروء

Run46 (35455984545) بنى native للمعماريتين، لكنه توقف قبل Kotlin بسبب `Code changed without updating docs/CHANGELOG.md`. قُرئ سجل job105933959708 وحزمة10588438470، SHA256=0ecdeb47449c88dbb631d6c4bc7da7fa7e44d22286ed12ae57703166044ffd22. الحزمة لا تحتوي نتائج اختبارات، فقط المصدر. لا نجاح مفترض للست عشرة حالة واجهة أو محول Rich.

قُرئت AGENTS والذاكرة والبنية والخارطة والقرارات والسجل والأمان والقدرات وخطة0006 والمصدر والاختبارات. المخطط المحلي td_api.tl SHA256=326b65b41442901ad6bf0ca2f7c356ae54365d6c343956a62e06a8b3cb305e87، من TDLib المثبت d1085f9c.

## الخطوات المرتبة

1. استكمال السجل والذاكرة والمواصفات بدون إزالة بوابة التوثيق. تحديثات الفرع ترفع كمجموعات مترابطة، لا commit لكل سطر يشغل native من جديد.
2. مراجعة التحويل قبل العرض: حدود فعلية أثناء اجتياز PageBlock وRichText لا بعد إنشاء شجرة غير محدودة؛ حفظ style للأزرار العادية والغنية؛ الحفاظ على البيانات الثنائية الأصلية. دعم جلب getFullRichMessage عند is_full=false مع عزل الجيل ومراجعة المحتوى، ومنع استجابة قديمة من استبدال تعديل أحدث.
3. فحص RichText: الروابط والأزرار داخل النص لا تنفذ مباشرة من الواجهة، بل أفعال مرتبطة بالرسالة عبر ActionTicket. spoiler يخفي المحتوى فعلًا قبل الكشف. لا تنفيذ HTML أو ملفات/دفع/روابط دخول خاصة.
4. تصحيح أحجام الفقاعات ومحاذاة الصادر والوارد وترتيب صفوف الأزرار في RTL؛ صفوف البروتوكول لا تعكس حسب لغة الجهاز. الحفاظ على وقت من date لا وقت مصطنع، والبيانات الوصفية في سطر صغير، وأهداف لمس واضحة دون التضحية بمساحة المحادثة. المكتبة والمظهر والأيقونة ومفاتيح البيانات لا تتغير.
5. تشغيل JVM والبناء وlint وnative/app tests دون تقليل الشروط. إضافة أدلة مصورة للمحادثة الغنية والمبدّل والفقاعات الفاتحة/الداكنة، بدل الاكتفاء بصور المعاينة القديمة. كل خلل مكتشف يصلح بخطوة موثقة قبل إعادة التشغيل.
6. توثيق النتائج الفعلية وحدود المعاينة الجديدة. بطاقات metadata للوسائط ليست تشغيلًا أو تنزيلًا؛ الصيغة الرياضية الخام ليست محرك تنضيد؛ لا ادعاء دعم Rich الكامل أو streaming/MiniApps.
7. تجهيز التسليم المهيأ من مصدر اجتاز CI فقط. حماية استمرارية التوقيع: لا تبديل شهادة0.3 أو طلب حذفها بصمت. المفتاح الخاص القديم غير موجود في الملفات المركبة حاليًا؛ يراجع مسار النسخة الاحتياطية قبل أي قرار توقيع. تغيير شهادة/معرف تثبيت يحتاج بيانًا واضحًا، ولا تعتبر حزمة CI غير المهيأة APK المطلوب.

## القبول والتراجع

القبول: زوال fallback للرسالة النصية الغنية، صفوف الأزرار خارج فقاعة المحتوى، فقاعة /start صغيرة، وقت صحيح، مبدّل صغير يعمل، بقاء اختبارات الحساب والجلسة والمكتبة والمظهر والكيبورد. تحفظ نتائج XML واللقطات ويقرأ الفشل قبل التصحيح. التراجع عن شريحة التصحيح فقط دون محو جلسة أو مكتبة ودون force-push.

المراجع: مخطط TDLib المثبت أعلاه؛ https://developer.android.com/develop/ui/compose/text/user-interactions ؛ https://developer.android.com/reference/kotlin/androidx/compose/ui/text/LinkAnnotation.Clickable .
