# 0007-E — مصفوفة Rich والبث وبوابة تسليم المالك

الحالة: complete ضمن بوابات CI والمحاكي والتوقيع المحلي؛ قبول هاتف المالك منفصل. 24 سبتمبر 2026. الفرع `feat/media-profiles-streaming` / PR6. نقطة البداية `b4d08dc10dc80077bf948672f75ecd9584e908f5` بعد اجتياز CI85. هذه الخطة تكمل `0007-media-profiles-streaming.md` و`0007-outgoing-media-execution.md` ولا تعيد الشرائح A–D.

## المطلوب ومعيار القبول

1. مطابقة كل أنواع `RichText` و`PageBlock` في مخطط TDLib المثبت `d1085f9c` مع المحول والعرض والاختبارات، وتصنيف كل نوع: مكتمل ضمن نطاق محدد، جزئي، أو غير منفذ. يُذكر البديل والحد الفعلي ولا تُنسب صورة أو نص مبسط إلى قدرة مكتملة.
2. اختبارات مستقلة لبث `messageText` وبث `messageRichMessage` وتعديل رسالة نهائية، مع عزل الحساب/المحادثة/المراجعة والرد القديم والانقطاع، ودون تفعيل أفعال المؤقت.
3. تشغيل الفحوص ذات الصلة وبوابة Android الكاملة على commit محدد، وقراءة XML والـlogs واللقطات؛ تحديث الذاكرة والسجل والخارطة والقدرات ودليل الاختبارات بنتائج هذا المصدر فقط.
4. بعد اجتياز المصدر، تجهيز owner delivery بتوقيع الشهادة الثابتة وهوية `com.ahmed9461.botos.app` وترقية رقم الإصدار دون uninstall أو كشف أسرار. تسليم APK فقط بعد تحقق التهيئة والبصمة والتوقيع والترقية الاصطناعية؛ لا يُنسب ذلك إلى تجربة حساب المالك.

## نقطة البداية والمصدر

CI85/35902432267 على `b4d08dc` نجح. الحزمة `BotOS-runtime-checks-85`/10770880004 طابقت SHA256 `2435312cd4cf3d3c38de936269463373d507d7523809f8adaf156e4cef2022c7`. تقارير XML: 102 JVM + 8 native + 77 app، صفر فشل/خطأ/تخطي، 21 لقطة، IME 6.095238dp، lint المحرك 0/0 والتطبيق 0 أخطاء/36 تحذيرًا، و`accountUsed=false`. لقطة التسجيل الداكن RTL روجعت. يغلق هذا D5 ضمن اختبارات المحاكي؛ ميكروفون هاتف المالك لم يُختبر هنا.

المخطط من حزمة العقد الموثوقة في نفس التشغيل، وSHA256 `td_api.tl` هو `326b65b41442901ad6bf0ca2f7c356ae54365d6c343956a62e06a8b3cb305e87`. مصدر التحويل `BotMessages.kt`، المصدر المؤقت `PendingReplies.kt`، الاختزال `BotMessageReducer`، والعرض `MessageRenderer.kt`/`ReceivedMediaUi.kt`. اختبارات النواة الحالية في `RichAdapterRegressionTest`, `PendingRepliesTest`, `BotMessagesTest`, `BotConversationsTest`، واختبارات العرض في `RichChatRegressionTest` وغيرها.

## الخطوات المرتبة

- [x] التحقق من CI85 وحزمة المصدر والـXML واللقطة وإغلاق D5.
- [x] استخراج الأنواع من المخطط المثبت، وإنشاء مصفوفة ذات صف لكل نوع وحد، مع فحص آلي يرفض نقص الصفوف أو تكرارها. محليًا 30/36 بالضبط من حزمة عقد CI85.
- [x] إضافة اختبارات البث والتعديل والفصل بين الحسابات والردود القديمة؛ اجتازت الحالات الثلاث في CI86.
- [x] تشغيل فحوص المستودع والاختبارات الممكنة محليًا، ثم CI على commit محدد وقراءة الحزمة وXML واللقطات. بوابة E التقنية اجتازت CI86، وتبقى بوابة إصدار المالك منفصلة.
- [x] تحديث الوثائق والقدرات والنتائج، ثم اجتياز owner delivery وفق `docs/OWNER_DELIVERY.md` وخطة 0007 الأصلية دون تغيير الهوية أو المفتاح.
- [x] فحص diff والأسرار والملفات غير المقصودة، commit وPR6 محدث قابل للمراجعة. لا merge ولا force-push.

## الاعتماديات والملفات والمخاطر

الملفات المتوقعة: `docs/TELEGRAM_CAPABILITIES.md` ومصفوفة جديدة، فحص Python في `scripts/` واختباراته، اختبارات `core:telegram`، وربما `BotMessages.kt` أو `PendingReplies.kt` إن أثبتت الاختبارات عيبًا. ملفات التسليم وGradle لا تتغير قبل نجاح E التقني. بعده تحديث الإصدار إلى 0.5.0-preview في `app/build.gradle.kts`، وأسماء حزمة التكامل في workflow، وحارس فرع التسليم في `.github/workflows/owner-delivery.yml` و`scripts/owner_delivery.py`، واختبارات `scripts/tests/test_owner_delivery.py` و`test_owner_packaging.py`، و`docs/OWNER_DELIVERY.md`. يبقى `delivery/request.json` معطلًا حتى اجتياز المصدر النهائي.

المخاطر: خلط مخطط Bot API مع TDLib، إظهار فعل قديم أو فتح رابط بلا موافقة، ادعاء دعم ترميز/رياضيات/مرجع لم يُعرض فعليًا، وإصدار مهيأ بلا توقيع المالك أو مع مسح بيانات النسخة 0.4. التخفيف: المخطط المثبت والبصمة، fixtures مصطنعة، اختبارات مراجعة وهوية، وبوابة CI والتوقيع الدائم خارج Git. عند فشل بوابة، وثق السبب وخطة تصحيح محددة قبل الكود، واترك آخر APK للمالك دون استبدال.

## التراجع

كل تعديل في الفرع وPR6. إن فشل المصدر أو التسليم، لا تنشر APK ولا تحذف بيانات أو ملفات مستخدم؛ أصلح السبب المحدد أو اعكس commit هذه الشريحة مع إبقاء دليل الفشل. لا إعادة إرسال تلقائي للمرفقات غير المؤكدة.

## فحص محلي قبل CI

`python scripts/check_rich_coverage.py --schema <td_api.tl من حزمة CI85>` نجح: 30 RichText و36 PageBlock. فحص التباين 14/14 و`check_ui_contract.py` نجح بعد ضبط نهايات أسطر XML المعتمدة في نسخة Windows دون تغيير Git. `python -m unittest discover -s scripts/tests -v` لا ينجح على Windows الحالي: عشرة اختبارات SDK تستدعي `bash` الذي يفتح WSL غير المثبت، واختبارا OpenSSL/symlink يتطلبان أدوات/امتيازات Linux؛ هذه ليست نتيجة CI. لا Android SDK/JDK محلي ظاهر. بوابة Linux CI مطلوبة قبل أي قبول.

## إغلاق E التقني قبل تسليم المالك

CI86/35910856881 على `5a009413b6ee60283992d5a07bb303027f661f86` اجتاز. حزمة BotOS-runtime-checks-86/10773497880 طابقت SHA256 `848f7e6a2e732f41b475d168e9219bfc5be6deea8f242c374cb91e1b00b0c6f4`. XML: **105JVM (8model+97telegram)+8native+77app** بلا فشل/خطأ/تخطي. أسماء اختبارات البث النصي والغني والتعديل وعزل التبويب/الانقطاع موجودة في XML وناجحة. 21لقطة بينها التسجيل الداكن RTL روجعت، IME6.095238dp، lint المحرك0/0 والتطبيق0أخطاء/36تحذيرًا، native `accountUsed=false`. فحص المصفوفة 30/36 اجتاز في خطوة العقد على مخطط native المثبت. لا يثبت هذا نسخة مهيأة أو ترقية المالك؛ يبدأ الآن تحديث إصدار 0.5.0-preview وحارس التسليم قبل CI النهائي.

مصدر إصدار0.5.0-preview الآن مكتوب: versionCode5، وتثبيت هويةالتطبيق، وحارس exact branch الحالي في workflow والمحقق، وفحص versionCode/versionName قبل تغليف APK، واسم payload الجديد. طلب التسليم ما زال `enabled:false`. ثمانية اختبارات Python محددة للحارس والتغليف نجحت على Windows؛ CI الكامل للمصدر لم يُقرأ بعد. اختبار إعادة تثبيت CI الحالي ما زال لنفس الإصدار، ولا يثبت ترقية0.4 إلى0.5 أو توقيع المالك النهائي؛ لا يدّعى خلاف ذلك.

## بوابة ترقية الإصدار قبل طلب التسليم

مراجعة عقد `verify_inplace_update.py` أظهرت أنه يعيد تثبيت APK الإصدار نفسه؛ هذا لا يحقق معيار E لترقية0.4→0.5 دون uninstall. بعد CI87 للمصدر الحالي: ابنِ ownerPreview 0.4 من commit المالك المجتاز `84ff6ef85a5185f0fee0b955b8ff13009dbd5b33` داخل worktree مؤقت في job التسليم، بلا أسرار Telegram وبنفس شهادة CI المؤقتة، مع native outputs المثبتة. تحقق من metadata للهوية/versionCode4، ثم ثبّته على المحاكي واكتب علامتي ملف وتفضيلات مصطنعتين باختبار probe، وثبّت ownerPreview 0.5 المهيأ بشهادة CI نفسها عبر `adb install -r` **دون uninstall**، ثم افحص بقاء العلامتين. يجري هذا داخل job الخاص فقط؛ لا تنشر APK القديم أو الجديد المهيأ علنًا. عدّل تقرير `owner-update.json` ليقول ترقية رقم إصدار مع مصدر/هدف واضحين، وغيّر محقق التغليف واختباراته لرفض تقرير reinstall قديم. أضف اختبارات فشل version/signer/missing previous APK. احتفظ بدليل أن هذا بشهادة CI المؤقتة، وليس إثبات التوقيع النهائي على هاتف المالك. بعدها فقط فعّل طلب التسليم من commit نهائي مجتاز.

تنفيذ البوابة يستعمل `git archive` في مجلد مؤقت بدل worktree؛ مصدر 0.4 محدد بالـSHA نفسه ولا يحتاج إلى تعديل Git metadata. يبني baseline قبل حقن أسرار التطبيق، ثم يجري probe قبل Gradle connected test كي لا يُثبت 0.5 أولًا على المحاكي. فحص metadata والتوقيع يسبق أي تثبيت، ويقبل التغليف تقرير ترقية 0.4→0.5 فقط. اختبارات Python محلية للترتيب ورفض التوقيع المختلف نجحت؛ بوابة CI للمصدر المعدل لم تُقرأ بعد.

CI88/35916568481 على `c59350836c2b14285e65500094a6c5849baf04e3` اجتاز بعد تعديل بوابة الترقية؛ حزمة10775636751 طابقت SHA256 `9e72a377eeaf7d0e7c9e91aba52bc03838dd59aaade7ea3f5a2d938ff72fbef9`. XML:105JVM+8native+77app بلا فشل/خطأ/تخطٍ، 21لقطة وIME6.095238dp، lint التطبيق0أخطاء/36تحذيرًا والمحرك0/0، `accountUsed=false`. تبقى تجربة الترقية الفعلية وبناء المالك الخاص داخل owner delivery؛ طلب المصدر CI88 فقط، بلا تغيير كود بعده.

## فشل owner delivery 35917995670 وخطة تصحيحه

طلب المصدر CI88 قُبل، وبناء baseline 0.4 نجح قبل حقن أسرار التطبيق. في خطوة المالك نجح probe الفعلي: `owner_upgrade_0.4_to_0.5=passed` على المحاكي بشهادة CI المؤقتة ودون حساب. بعده أعاد Gradle حزم APK عند `connectedOwnerPreviewAndroidTest`، فرفض Android تثبيته فوق الحزمة السابقة بـ`INSTALL_FAILED_UPDATE_INCOMPATIBLE`؛ لم يُنتج CMS أو APK مسلّم. `run_ui_tests.sh` يغيّر `ANDROID_SDK_HOME` و`ANDROID_USER_HOME` لمسار AVD المؤقت؛ إنشاء debug keystore في هذا السياق يجعل توقيع إعادة الحزم مختلفًا عن بناء المالك السابق. التصحيح المحدود: احتفظ بهذه المتغيرات للمحاكي فقط، واستدع Gradle connected test ببيئة Android user/home الأصلية بعد نجاح probe؛ يبقى `ANDROID_HOME` و`ANDROID_SERIAL` ثابتين. أضف اختبار عقد يرفض وراثة مسار AVD إلى Gradle، ثم عطّل الطلب القديم قبل رفع الكود. بعد CI للمصدر الجديد، فعّل طلبًا جديدًا يشير إلى commit وتشغيله، واقرأ تقرير startup وowner-update وCMS. لا تغير شهادة المالك أو هوية التطبيق، ولا تلغِ اختبار الترقية.

CI90/35919623482 على `0a7cb2d854ba2e7117268997026e1b0e7926852b` اجتاز التصحيح؛ حزمة10777176724 طابقت SHA256 `a92f4b9ff858fa3674dea8a79ef840b5d358dbc3ae539d309a580495b91365dd`. XML:105JVM+8native+77app بلا فشل/خطأ/تخطٍ، 21لقطة وIME6.095238dp، lint التطبيق0أخطاء/36تحذيرًا والمحرك0/0، `accountUsed=false`. هذا يؤهل طلب owner delivery جديدًا من المصدر المطابق؛ نتيجته منفصلة.

## الإغلاق — owner10 والتوقيع النهائي

owner delivery 35921701021 اجتاز من المصدر CI90، مع بناء baseline 0.4، وprobe ترقية 0.4→0.5 الفعلي بلا uninstall وبقاء العلامتين، وOwnerBuildSmokeTest 1/1، ثم نشر CMS مشفرة فقط. حزمة10777801768 طابقت SHA256 `1a078f4ebeb7e5f8f730e34fa0782a88c5cddc78dfe80994f72b3ff0707a0114`. فُكت محليًا بمفتاح الاستعادة الموجود خارج Git، وتحققت provenance والتقارير والبصمات. وُقّع APK 0.5 بالشهادة الدائمة المطابقة لنسخة0.4؛ SHA256 النهائي `30b1286f896bfaea0bda9aa9b2194c14dae015b248c3bc248929cc90dbf71745`، 91,034,525 بايت، ونجح تحقق apksigner v2 ومقارنة محتوى ZIP غير التوقيعي. تقرير التحقق بجانب APK خارج Git، وأُغلق طلب التسليم. PR6 محدث للمراجعة. لا دليل على تثبيت نسخة0.5 بالتوقيع النهائي على هاتف المالك أو استخدام حسابه؛ هذا قبول ميداني لاحق، ولا يعيد فتح اختبارات CI الناجحة.
