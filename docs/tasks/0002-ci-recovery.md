# المهمة 0002 — استكمال التحقق من بناء Android

التاريخ: 2026-09-18. الحالة: in_progress. هذه مهمة تابعة لبوابة التحقق في [0001](0001-foundation.md)، وليست بدءًا جديدًا للتطبيق.

## نقطة البداية والأدلة

الفرع feat/android-foundation عند 0ac337e6507a04ab672190a7f3eb21ffe97cba69، PR #1. main يحتوي التخطيط، والفرع يحتوي التطبيق؛ يجب فحص الفروع لا افتراض أن main يمثل كل العمل.

تمت قراءة AGENTS والذاكرة والبنية والخارطة والقرارات والتصميم والأمان والقدرات وخطة0001، ثم فرق PR وملف workflow وسجل job105412687739.

Run35284171101 فشل في خطوة Install Android platform: `sdkmanager: command not found` (127). نجح فحص المستودع و14 زوج تباين؛ لم تبدأ اختبارات Gradle أو lint أو APK. المصدر: https://github.com/ahmed9461/BotOS/actions/runs/35284171101

## الخطوات بالترتيب

- [x] 1. اكتشاف فرع التنفيذ وقراءة نقطة التوقف والسجل وتوثيق السبب قبل التعديل.
- [ ] 2. فصل تجهيز SDK في سكربت يفحص SDK_ROOT وأداة sdkmanager صراحة، لا يعتمد على PATH ولا يخفي فشل التنزيل/الرخص.
- [ ] 3. اختبار السكربت محليًا باستخدام SDK مصطنع: المسارات ذات الفراغات، فقد الأداة، تضارب الجذور، انتشار رمز الفشل، وعدم تشغيل أداة من SDK مختلف.
- [ ] 4. وصل السكربت بالـworkflow وإعادة تشغيل CI من تحديث PR، وقراءة النتيجة. إذا ظهر خطأ جديد حدّث هذه الخطة أولًا ثم أصلح سببًا محددًا.
- [ ] 5. سجل نتائج الاختبارات والبناء الفعلية والملفات الناتجة، وحدّث الذاكرة والسجل ودليل البناء. لا تعتبر اختبار محاكاة SDK اختبار Android.

## الملفات وحدود المهمة

scripts/install_android_sdk.sh، scripts/tests/test_android_sdk.py، .github/workflows/android.yml، docs/PROJECT_MEMORY.md، docs/CHANGELOG.md، docs/BUILD.md، وهذه الخطة. لا تغيير الإصدارات أو التطبيق لمجرد فشل PATH؛ أي مشكلة بناء لاحقة تحتاج دليلًا وخطة مصغرة.

## معايير القبول والتراجع

اختبارات السكربت ناجحة، انتهاء CI إلى test/lint/assemble فعليًا مع نتيجة موثقة، لا تعطيل فحوص ولا continue-on-error على البناء. إذا تعذر SDK نفسه يسجل مانع واضح بدل نجاح كاذب. لا دمج قبل المراجعة. التراجع بـrevert commit الإصلاح دون المساس بأساس التطبيق أو التخطيط.

مرجع SDK: https://developer.android.com/tools/sdkmanager
