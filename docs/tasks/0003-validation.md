# ملحق التحقق للمهمة 0003

## المحاولة 7 — 2026-09-18

التشغيل: https://github.com/ahmed9461/BotOS/actions/runs/35291421156
الكود: 97ba61aee642469df75998f449428cb0d64624db.

قُرئت سجلات العمل 105434887945. نجحت فحوص المستودع والأيقونة والتباين الأربعة عشر واختبارات Python الثلاثة عشر واختبارات النواة. نجح تجهيز SDK وتوليد Wrapper. توقف تجميع التطبيق بخطأين في استخدام WindowInsets.isImeVisible، لأنه يتطلب قبول ExperimentalLayoutApi في إصدار Compose المثبت. لم يبدأ اختبار المحاكي؛ لا صور أو APK مثبتة لهذه الجولة بعد.

## خطة الإصلاح قبل تعديل المصدر

1. قبول واجهة isImeVisible صراحة في الدالتين اللتين تحتاجانها فقط، دون تعطيل فحوص المترجم أو lint. الاعتماد نفسه إصدار مستقر مثبت؛ علامة التجريب تخص هذه الواجهة البرمجية فقط.
2. تلوين خلفية الجذر قبل تطبيق مساحات النظام حتى لا تظهر خلفية XML القديمة حول سطح التطبيق الجديد.
3. حماية استخدام LocaleManager في اختبار الجهاز بشرط API 33 أو أعلى؛ لا تغيير الحد الأدنى للتطبيق.
4. إعادة التجميع وlint ثم اختبار المحاكي. لا تغيير معايير القبول أو إخفاء فشل. حفظ لقطات الجهاز وقياس الفراغ كما هو مخطط.

هذه تعديلات محدودة ضمن المهمة 0003. ملفات الأيقونة والتخزين محفوظة. تحديث الوثائق المقروءة يرافقها، ثم تُسجل نتيجة التشغيل التالي بعد قراءتها.
