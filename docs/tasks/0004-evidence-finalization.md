# ملحق 0004 — حفظ دليل الجهاز وإغلاق التحقق

2026-09-18. خطة قبل تعديل ملفات البناء والتحقق. الحالة: planned.

## ما ثبت بالفعل

نجح التشغيل35375219097 على182e57ec. نُزلت حزمة10560231229 وتطابقت بصمة الأرشيف، وقرئت التقارير:21حالة JVM، وثماني حالات JNI/Keystore، وثلاث حالات واجهة، بلا فشل أو تخطي. lint وحدة المحرك بلا أخطاء أو تحذيرات. تحتوي AAR مكتبات المعماريتين وبصماتهما مطابقة للبناء المثبت.

سجل المهمة105698286130 عرض من الجهاز: version=1.8.67 وcommit=d1085f9cebc5a62379991ae1652673954f229c1f وunicodeRoundTrip=passed وaccountUsed=false. ملف native-runtime.txt فُحص داخل CI لكنه لم يدخل الأرشيف النهائي لأن مجلد additional_output لم يكن ضمن مسارات الرفع. النتائج ناجحة فعلًا، والنقص في حفظ الدليل كملف مستقل وليس فشلًا مخفيًا للاختبار.

## الخطة

1. نسخ ملف دليل الجهاز إلى diagnostics بعد التحقق منه، ومقارنة commit الذي أعاده الجهاز بالمصدر المثبت. لا توليد دليل بديل يدويًا.
2. استخدام tasks.register باسم صريح بدل delegate registering المتقادم، وفق تحذير Gradle9.6 المقروء؛ لا تغيير لمعيار نقص JNI أو تعطيل تحذير.
3. إعادة تشغيل المجموعة نفسها، وقراءة native-runtime.txt من الأرشيف والتقارير واللقطات وAAR، ثم تحديث جميع حالات التوثيق والخطة والذاكرة وPR.

## القبول والحدود

لا تغيير لواجهة التطبيق أو الأيقونة أو البيانات أو منطق التفويض. جميع21حالة JVM و11حالة جهاز تبقى مطلوبة، واللقطات والقياس وشروط الأمان لا تُخفض. P2b للربط بالحساب ما زال منفصلًا. التراجع عبر revert لهذا التصحيح فقط. لا ادعاء دعم Gradle10 أو اختبار حساب حقيقي.
