# تنفيذ الخطوة الأولى من 0005 — منسق الحساب

2026-09-18. الحالة: validating، لا نجاح اختبارات مفترض. الخطة الأصلية في c67f38de سبقت هذا الكود. لا تغييرات لواجهة التطبيق أو الأيقونة أو الاعتمادات.

## المنفذ

AccountSession وAccountSessionFactory يفصلان منسق الحساب عن JNI وAndroid في اختبارات JVM. AndroidTelegramSession يطبق الواجهة مع الحفاظ على التفويض وحماية الملفات والملكية. AccountCoordinator يطلب التهيئة والموافقة قبل فتح عميل، ويستخدم تحديثات AuthStep فقط لتقدم الدخول، ثم getMe للتحقق من هوية الحساب. حقول الكود وكلمة المرور لا تدخل AccountState أو سلاسل التشخيص.

الاستعادة لا تفتح جلسة في تثبيت جديد، وتفحص الإذن مرة واحدة. جيل الجلسة يمنع استجابة كائن قديم من تغيير حساب أحدث. إلغاء دخول غير مكتمل يغلق المكتبة ولا يدعي خروجًا من الخادم أو يمحو الملفات. طلب الخروج يعطل الاستعادة قبل محاولة الإلغاء، وكتابات الإذن متسلسلة لمنع جواب getMe متأخر من إعادة تفعيلها. فشل الخروج لا يظهر نجاحًا، وفشل التنظيف بعد Closed له حالة مستقلة.

## التحقق المطلوب

كتبت15حالة JVM جديدة تغطي غياب التهيئة والموافقة، التثبيت الجديد والاستعادة، الرد Ok والمرحلة القديمة، هوية الحساب ومهلتها، فشل فتحالمكتبة والتهيئة، الإلغاء والتزامن، الخروج الفاشل والناجح وفشل التنظيف، وسباق حفظ إذن الاستعادة. تستخدم جلسات ومفاتيح مصطنعة ولا تتصل بخوادمTelegram.

شغّلها مع الحالات21 السابقة، ثم native8 وUI3 وlint/build وقراءة الأدلة. لا تعتبر وجود هذهالملفات نجاحًا. لا يضاف AccountCoordinator إلى app ولا حقول دخول شكلية في هذه الخطوة؛ التهيئة والشاشة وربط البوتات تأتي بالترتيب التالي في0005.

## الخطوة التالية

اقرأ نتائج CI لهذا التغيير، عالج أي فشل دون خفض الشروط، ووثق. بعد النجاح: تنفيذ تهيئة آمنة وشاشةالحساب وربطها بالمنسق، مع حفظ الإذن في ملف DataStore منفصل وحماية المدخلات وقواعد النسخ. بيانات التطبيق خارجGit؛ لا طلب OTP أو كلمة المرور في المحادثة.
