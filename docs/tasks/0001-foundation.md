# المهمة 0001 — تأسيس BotOS وأول شريحة Android

الحالة: **completed — نطاق الأساس والمعاينة فقط**. 2026-09-18. الفرع feat/android-foundation، PR#1؛ ليس اكتمال المنتج الحي أو مراجعة الجهاز.

## المطلوب والحدود

تخطيط وذاكرة وقواعد إلزامية قبل التنفيذ، بنية Android قابلة للاختبار، RTL وDark/Light/System، تنظيم تبويبات وأسماء، renderer ومعاينة منفصلة، CI ووثائق دقيقة. لا نجاح وهمي للاتصال ولا إكمالP2 ضمن هذه المهمة.

## قراءات ما قبل التنفيذ

AGENTS والذاكرة والبنية والخارطة والقرارات والتصميم والأمان ومصفوفة القدرات والاعتمادات. فُحص المستودع قبل الإنشاء، وسبقت وثائقP0 الكود. عند الاستئناف قُرئ الفرع وPR والخطط بدل افتراض أن main يحتوي كل العمل.

## الخطوات المنفذة

- [x] 1. فحص وتهيئة وكتابة الذاكرة والخطة قبل التطبيق.
- [x] 2. فرع تنفيذ وإصدارات موثقة ونماذج واختبارات مستقلة.
- [x] 3. Gateway ومعاينة غير متصلة وDataStore للتفضيلات/bookmarks.
- [x] 4. كتابة الثيمات والأيقونة والشاشات والتنقل ومحرك المحتوى؛ لا ادعاء فحص جهاز.
- [x] 5. تشغيل CI وقراءة أخطائه، إصلاحه بخطط0002، ثم JUnit/lint/assemble ناجحة في run35288442618.
- [x] 6. قراءة التقارير والتحقق من APK وبصمته وتحديث الوثائق والحدود وPR وتسليم معاينة قابلة للتتبع.

## الأدلة

الكود المختبر7724a5fbc1459aac58b8d829f6e4ded1b8281f71. JUnit4 ناجحة (النواة37 assertion داخل حالة جامعة +3 للمعاينة)، Python13، contrast14، lint0 أخطاء و9 تحذيرات موثقة، assembleDebug ورفع الحزم ناجحة. التفاصيل والبصمة في [TESTING](../TESTING.md). المصدر: https://github.com/ahmed9461/BotOS/actions/runs/35288442618

## قرارات التنفيذ

AGP9.3.1 مع Kotlin2.4.20 وJDK21/Gradle9.6.1 بناءً على التوافق. لا تنزيل Wrapper مخصص؛ توليد رسمي. لا جمع هاتف/رمز قبل وجود TDLib adapter. الحفاظ على صفوف أزرار الرسائل وحدود الحجم/العمق وعزل الأحداث القديمة. إصلاح اسم حزمةSDK إلى37.0 بعد قائمة فعلية لا تخمين.

## المتبقي خارج هذه المهمة

اختبار الواجهات على جهاز والخط الكبير وTalkBack وتغيير اللغة والأداء، إكمال تحسينات الأيقونة وقواعد نقل البيانات، تثبيت Wrapper المولد في Git بعد مراجعته، ثم خطةP2 للتكامل الحقيقي. لا تسجيل دخول/وسائط/streaming حي في المعاينة. PR مفتوح ولم يدمج حتى هذه النقطة.

## التراجع

التاريخ محفوظ؛ لا force أو حذف. يمكن revert شريحة التطبيق دون حذف تخطيطP0. لا تخلط APK debug بإصدار إنتاج أو دليل سلاسة.
