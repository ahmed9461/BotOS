# المهمة 0001 — تأسيس BotOS وأول شريحة Android

الحالة: **validating**. التاريخ2026-09-18. الفرع feat/android-foundation، base ca4776f5f9bdf7cdac757daddb036a9722776c22. الطلب: وضع التخطيط والذاكرة داخل المستودع والبدء بالتطبيق.

## المطلوب ومعيار القبول

بناء Android قابل للاختبار، واجهة أصلية RTL وDark/Light/System، تنظيم أسماء وترتيب التبويبات، renderer ورسائل/أزرار ديناميكية بمعاينة صريحة، فصل UI/data، اختبارات وCI ووثائق دقيقة. لا نجاح وهمي للاتصال ولا ادعاء إكمال P2.

## قراءات ما قبل التنفيذ

AGENTS، الذاكرة والبنية والخارطة والقرارات والتصميم والأمان ومصفوفة القدرات والاعتمادات. تم فحص المستودع الفارغ قبل الإنشاء، وكتابة الخطة والتوثيق في commit سابق لكود التطبيق، ثم قراءة الذاكرة من فرع العمل. لا كود مستخدم قائم لاستبداله.

## الخطوات المرتبة

- [x] 1. فحص وتهيئة وكتابة الخطة والذاكرة قبل التطبيق.
- [x] 2. فرع تنفيذ وإصدارات موثقة، core models واختبارات مستقلة.
- [x] 3. حدود Gateway ومعاينة غير متصلة وDataStore للتفضيلات/bookmarks.
- [x] 4. تصميم وثيمات وأيقونة وواجهات وتنقل وrenderer؛ مكتوبة ولم تُراجع على جهاز بعد.
- [ ] 5. تشغيل CI واختبارات Gradle وlint وAPK، وقراءة النتيجة ومعالجة الفشل.
- [ ] 6. مراجعة نهائية ونتائج ووثائق وPR وتسليم.

## الملفات والأثر

app + core/model/telegram/data/designsystem، Gradle catalog، scripts فحص التوثيق/الألوان، workflow وPR template، وثائق البناء والاختبار والذاكرة والسجل. لا مساس بأي مستودع أو خدمة أخرى.

## تغييرات الخطة المبررة

بعد قراءة مصفوفة Kotlin: اختيار AGP9.3.1 بدل9.4.0 المرصود أولًا، وJDK21 وGradle9.6.1. استخدام إجراء Gradle الرسمي وتوليد Wrapper بدل مشغل تنزيل مخصص. توفيرالمعاينة فقط في P1 بدل جمع هاتف/رمز قبل وجود adapter حقيقي. الحفاظ على صف أزرار الرسالة دون تقسيم يغير ترتيبها، وحدود عمق/حجم لحماية العرض.

## الأدلة المحلية

شُغّل kotlinc على core/model وCoreChecks.kt مع include-runtime، ثم java -jar: 37 core checks passed. شُغّل scripts/check_contrast.py: 14 زوجًا نجح >=4.5:1. اختبارات النواة تشمل تطبيع الروابط والأسماء والتكرار والترميز وعزل الحساب/البوت وتعديل الرسالة ورفض الأفعال القديمة وحدود الحالة.

## فحوص لم تُشغّل بعد

Gradle/JUnit الأربعة وAndroid lint/build بانتظار CI. UI على جهاز وTalkBack والخط200% وFPS غير متحققة. لا TDLib/login/media/streaming في هذه الشريحة.

## التراجع

PR وفرع محفوظان؛ لا force ولا حذف تاريخ. إذا فشل البناء يبقى مسجلًا ولا يُسلم APK على أنه جاهز. يمكن revert شريحة التنفيذ دون حذف وثائق P0.

## نقطة الاستئناف

افتح PR وشاهد workflow jobs/logs ثم حدّث الأدلة هنا وTESTING والذاكرة. إنشاء workflow ليس إكمال الخطوة5.
