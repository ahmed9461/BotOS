# تحقق native ضمن المهمة0004

2026-09-18. نجح بناء المعماريتين فعليًا في35372781224. نُزل artifact10559053421 لـx86_64 وتطابقت بصمة الأرشيف والبصمات الداخلية وJava الرسمي. المكتبة ELF64 بمعمارية صحيحة وLOAD alignment=0x4000، ولا اعتماد OpenSSL ديناميكي غير مرفق. هذا ليس اختبار تشغيل بعد.

خطة0004 سبقت إضافة المحرك والاختبارات. لإتمام خطوتي5و6، أضيفت مهمة verify-runtime بعد نجاح مصفوفة native في نفس workflow. تحمل مخرجات نفس التشغيل، وتتحقق من lock والبصمات وschema وJava وELF، ثم تشغل JVM وlint وتجميع واختبارات JNI/Keystore على الجهاز نفسه مع اختبارات UI السابقة. بقيت اختبارات الواجهة وشروط المسافة والصور دون تغيير.

بوابة الأدلة تطلب ثماني حالات native/storage بلا فشل أو تخطي، وملف native-runtime.txt صادرًا من الجهاز، وسبع صور UI والقياس. كل مخرجات الفشل محفوظة، ولا اعتبار لمكتبة مبنية أو ملف AAR على أنه تطبيق متصل بالحساب.

## تصحيح التهيئة قبل الجهاز

المحاولة35374736478 اجتازت contract/ELF/الأيقونة والتباين، لكنها فشلت في تهيئة Gradle عند ClassCastException في AndroidLibrarySourceSet القديم. كتبت خطة7754bb82 قبل الإصلاح؛ تغير التسجيل إلى public variant API، مع preBuild ترفض نقص JNI عند بناء وحدة المحرك. لم تتغير مجموعة الإصدارات ولا اختبارات الجهاز ولم يعطل lint. الحالة validating حتى قراءة التشغيل التالي.
