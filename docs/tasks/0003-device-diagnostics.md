# تشخيص اختبار الجهاز — المهمة 0003

## النتيجة المقروءة للمحاولة 8

التشغيل 35291990328 على 708d7686. نجح التجميع وlint وحزمة الاختبار. بعد استبدال التشغيل وقراءة السجل ظهر السبب عند 00:42:15 UTC: `Unknown AVD name [botos-ui]` وغياب ملف botos-ui.ini في مسار بحث المحاكي. **لم يصل التنفيذ إلى اختبارات التطبيق.**

أظهر السجل خروج المحاكي، ثم استمر انتظار أوامر adb في تنظيف السكربت دون حد حتى ألغيت المحاولة بسياسة concurrency. ليست هذه نتيجة نجاح أو فشل للمفتاح أو الكيبورد؛ إنها مشكلة تجهيز للمحاكي وحدود الانتظار. حزمة الأدلة 10526858189 حفظت السجل ونتائج التجميع والنواة.

## خطة التصحيح قبل المصدر

1. توحيد ANDROID_USER_HOME وANDROID_EMULATOR_HOME وANDROID_AVD_HOME، مع ANDROID_SDK_HOME المتوافق للأدوات الأقدم، في مجلد مؤقت واحد. إبقاء مسار SDK وHOME الخاص بالبناء دون تغيير.
2. تمرير مسار AVD صريح إلى avdmanager، ثم التأكد أن emulator -list-avds يجد الاسم قبل الإقلاع. لا اختلاق مسار غير موجود أو تجاهل الخطأ.
3. تخصيص المنفذ والرقم التسلسلي للمحاكي، وتحديد مهل فعلية لتنزيل الحزم والإقلاع وكل أمر adb والاختبار والتنظيف. إذا مات المحاكي يفشل السكربت بسرعة ويحفظ السبب بدل الانتظار إلى أجل غير معلوم.
4. حفظ تقدم التجهيز والاختبار في diagnostics، وإعادة اختبارات التطبيق نفسها دون حذف أي معيار. نجاح التجميع وحده لا يسمح بتسليم APK بوصفه مختبرًا على جهاز.

المحاولة 9 بدأت بتحديث الوثائق؛ ستستبدل بالتصحيح بعد كتابته لأن مصدر التطبيق لم يتغير. تُقرأ نتيجة المحاولة المصححة قبل الإكمال.

## المصادر التي روجعت في 2026-09-18

- https://developer.android.com/tools/variables — مسارات تفضيلات الأدوات ومسارات بحث AVD والفرق بين الأدوات القديمة والجديدة.
- https://developer.android.com/tools/avdmanager — خيار تحديد المسار.
- https://developer.android.com/studio/run/emulator-commandline — خيارات الإقلاع والمنفذ.

لا تغيير في ألوان التطبيق أو الأيقونة أو التخزين في هذا التصحيح؛ هو تجهيز اختبار الجهاز فقط.
