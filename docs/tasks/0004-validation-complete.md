# إغلاق بوابة P2a — المحرك والجلسة

2026-09-18. الحالة: completed ضمن نطاق محرك مستقل لا حساب مستخدم. المصدر المختبر e9b45c60bd8de80d206146c49be0288d0245e464، وخطة تصحيح التعليق 8d25d289 سبقت التعديل. هذا تحديث أدلة فقط لا تغيير للمصدر.

## الأدلة المقروءة

- تشغيل المحرك https://github.com/ahmed9461/BotOS/actions/runs/35380280277 نجح كاملًا، بما فيه مصفوفة ABI والتحقق الحقيقي على المحاكي.
- حزمة BotOS-runtime-checks-8 رقم10562130090 نُزلت وفُحص ZIP وبصمته: 35b9fb907820bc726e949b8dab0b4a5c940bda0f8e7a4b6462ee5fee9c57e9f4.
- XML: 21 حالة JVM و8 حالات JNI/Keystore و3 حالات واجهة، دون فشل أو خطأ أو تخطي. core:tdlib lint صفر أخطاء وصفر تحذيرات.
- native-runtime.txt صادر من الجهاز: version1.8.67 وcommit d1085f9cebc5a62379991ae1652673954f229c1f، unicodeRoundTrip=passed، authorization=waitTdlibParameters، accountUsed=false.
- راجعت سبع لقطات فعلية: المظهر الفاتح/الداكن/بعد الاستعادة، المساحة، المكتبة، المحرر، الكيبورد. قياس الفاصل6.095238dp: rootHeight2400، imeBottom883، composerBottom1501.
- فُحص AAR حجمه26,917,143بايت؛ يحتوي JNI للمعماريتين: arm64 SHA256=45c4ea02a4aaaefd0d749b1cfddb328cd69a4de78379cebb3c6ae6c8cde12657 وx86_64 SHA256=af86156e2ebdcbdd85f99d414633df315e6452b0f9599d2d1d2070def8bee4ff.
- تشغيل Android المستقل https://github.com/ahmed9461/BotOS/actions/runs/35380280304 نجح أيضًا في التجميع وlint والجهاز وجمع الأدلة وإخراج معاينة0.2. ليس APK متصلًا جديدًا.

## سبب التوقف السابق وما تغير

35377793103 اجتاز JVM21 وnative8 لكن UI توقف عند0/3. استبدلت مزامنة runOnIdle المتداخلة مع getter النشاط بقراءة snapshot واحدة عبر ActivityScenario من خيط الاختبار. أعيدت نفس الشروط والأعداد، ونجحت في تشغيلين مستقلين. لم تُخف مهلة ولم يتغير كود الإنتاج أو التصميم أو الأيقونة. ملف0004-window-polling وخطط الأخطاء السابقة تبقى كسجل تاريخي؛ النتيجة الأحدث هنا.

## الحدود والانتقال

P2a مكتملة كطبقة نقل وجلسة مستقلة مختبرة. لا app→core:tdlib أو شاشة دخول أو استعمال بوت حقيقي داخل التطبيق بعد، ولا حساب/OTP/كلمة مرور استُخدمت. لا اختبار runtime16KB أو FPS أو أمان شامل. Wrapper مولد رسميًا ولم يثبت في Git؛ توقيع preview مؤقت.

التالي: خطة P2b على فرع مستقل قبل التنفيذ، لربط الحساب ومساحات البوتات مع موافقة صريحة وبيانات تطبيق خارج Git، دون تغيير الهوية المعتمدة. لا يساوي نجاح هذه البوابة اعتماد منتج إنتاجي أو إذن دمج تلقائي.
