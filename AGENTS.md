# BotOS — تعليمات إلزامية لكل منفّذ

هذه التعليمات تسري على جميع ملفات المستودع. لا تعتمد على محادثة سابقة بدل الملفات الحالية.

## قبل أي تنفيذ — بالترتيب، دون تجاوز

1. افحص الفرع و`git status` وآخر commits، واحفظ أي عمل قائم دون مسحه أو استبداله.
2. اقرأ `docs/PROJECT_MEMORY.md` لمعرفة **ما نُفّذ فعلًا، نقطة التوقف، المهمة النشطة، والموانع**.
3. اقرأ `docs/ARCHITECTURE.md` و`docs/ROADMAP.md` و`docs/DECISIONS.md` و`docs/CHANGELOG.md`، ثم المواصفات المتعلقة بالمهمة. لمهام الواجهة اقرأ `docs/UI_GUIDELINES.md`، ولمهام Telegram اقرأ `docs/TELEGRAM_CAPABILITIES.md` و`docs/SECURITY.md`.
4. اقرأ الكود الحالي في مسار المهمة والمستهلكين والاختبارات. افهم مصدر الحقيقة وتدفق الحالة وحدود الوحدات. **لا تخمن البنية أو حالة ميزة من اسم ملف.**
5. أنشئ أو حدّث خطة مهمة في `docs/tasks/` قبل تعديل التطبيق، تتضمن: المطلوب، نقطة البداية، الملفات، الخطوات المرتبة، الاعتماديات، المخاطر، الاختبارات ومعيار القبول والتراجع.
6. سجّل المهمة النشطة ونقطة البداية في الذاكرة. ابدأ التنفيذ فقط بعد اكتمال الفهم والخطة.

## أثناء التنفيذ

- اقرأ ← افهم ← خطط ← نفذ بالترتيب ← اختبر ← وثّق ← راجع ← سلّم.
- لا تتجاوز خطوة فاشلة أو تحول عنصرًا غير منفذ إلى مكتمل. عند ظهور معلومة تغير الخطة، حدّث الخطة وسبب التغيير أولًا.
- أنجز شرائح متكاملة صغيرة، لا ملفات واجهات معزولة أو أزرار وهمية. لا تُعد كتابة أجزاء غير مرتبطة بالمطلوب.
- لا تخلط الاتصال أو التخزين أو منطق الأزرار مع Composables. لا تستنتج callback من نص الزر.
- تحقق من الإصدارات من المصدر الرسمي وسجل التاريخ والرابط والتوافق؛ لا تعتمد رقمًا لأنه الأحدث منفردًا. امنع `+` و`latest` والاعتمادات المتحركة في ملفات البناء.
- أي معاينة محلية يجب أن تكون واضحة للمستخدم ومنفصلة عن الحساب الحقيقي. لا تُظهر نجاح اتصال أو إرسال لم يحدث.
- لا توكنات بوتات، لا بيانات جلسة، لا أكواد دخول، لا كلمات مرور في Git أو logs أو fixtures.
- كلام الواجهة للمستخدم العادي، والتشخيص والقرارات الهندسية داخل الملفات المناسبة. الإفصاح عن استخدام Telegram والخصوصية ليس نصًا تقنيًا زائدًا ويجب إبقاؤه.

## قبل اعتبار المهمة مكتملة

- شغّل الاختبارات ذات الصلة والبناء عند توفر البيئة. سجل الأمر والنتيجة ورابط CI أو ملف الدليل. الاختبار غير المشغّل = غير متحقق، لا نجاح ضمني.
- راجع RTL وDark/Light وحالة الانتظار والخطأ وBack والأزرار المتكررة وعزل البوتات، وفق نطاق المهمة.
- حدّث `PROJECT_MEMORY.md` و`CHANGELOG.md` وخطة المهمة. حدّث `ROADMAP.md` والمواصفات و`DECISIONS.md` عند تغيرها، في نفس مجموعة التغييرات.
- سجل **أول خطوة تالية قابلة للتنفيذ**، لا عبارة عامة مثل «أكمل المشروع».
- افحص diff والأسرار والملفات غير المقصودة، ثم commit موصوف وPR قابل للمراجعة. لا force-push ولا تجاوز للحماية أو حذف عمل المستخدم.

## بوابة الإكمال

المهمة غير مكتملة ما لم تتصل: المتطلبات → الخطة → الكود → الاختبار → الدليل → تحديث الذاكرة. إنشاء workflow وحده ليس دليلًا على نجاحه. وجود نص «سلس» ليس قياس أداء. لا يمكن لملف تعليمات ضمان أن كل منفّذ قد قرأه؛ المراجعة وCI يتحققان من آثار العمل، ولا يُدّعى غير ذلك.
